// Alpha Husky - Relay Fringe 01 production run. Local action-RPG combat; completion is intentionally isolated.
(function (global) {
  "use strict";

  const ROOT_ID = "alphaExplorationRoom", CANVAS_ID = "alphaExplorationRoomCanvas", STYLE_ID = "ah-exploration-room-css";
  const SECTOR = Object.freeze({ sectorId: "relay_fringe_01", displayName: "Relay Fringe 01", relayRequired: true, completionMode: "relay_activation", runProfileVersion: 1 });
  const WORLD = Object.freeze({ width: 5000, height: 1600, playerSpawn: { x: 220, y: 1210 }, relay: { x: 4760, y: 800 }, extract: { x: 220, y: 1280 } });
  const COMBAT = global.AlphaSectorCombatConfig?.makeCombatConfig?.({ playerSpeed: 230, playerAttackRange: 86, playerAttackCooldownMs: 360, enemyMaxHp: 78, enemyDamage: 7, enemySpeed: 92, enemyAggroRange: 260, enemyAttackRange: 48, enemyAttackCooldownMs: 920 }) || { playerSpeed: 230, playerAttackRange: 86, playerAttackCooldownMs: 360, enemyMaxHp: 78, enemyDamage: 7, enemySpeed: 92, enemyAggroRange: 260, enemyAttackRange: 48, enemyAttackCooldownMs: 920 };
  const SHOOTER = Object.freeze({ hp: 58, damage: 5, speed: 76, aggroRange: 430, preferredRange: 230, retreatRange: 130, projectileSpeed: 180, cooldownMs: 1650 });
  const SENTINEL = Object.freeze({ hp: 210, damage: 12, chaseSpeed: 48, telegraphMs: 700, lungeMs: 440, recoveryMs: 900, lungeSpeed: 430 });
  const LOOT = Object.freeze({ exp: { frame: 0, label: "EXP shard", value: 12, chance: 1 }, scrap: { frame: 1, label: "Scrap", value: 1, chance: .60 }, bones: { frame: 2, label: "Bones", value: 1, chance: .55 }, equipment: { frame: 3, label: "Equipment drop", value: 1, chance: .20 } });
  const ENCOUNTERS = Object.freeze([
    { id: "signal_approach", name: "SIGNAL APPROACH", required: true, trigger: { x: 590, y: 1180, w: 360, h: 500 }, gate: "approach", spawns: [{ type: "chaser", x: 620, y: 1090 }, { type: "chaser", x: 760, y: 1260 }, { type: "chaser", x: 870, y: 1030 }] },
    { id: "broken_lane", name: "BROKEN LANE", required: true, trigger: { x: 1800, y: 780, w: 540, h: 740 }, gate: "lane", spawns: [{ type: "chaser", x: 1660, y: 640 }, { type: "chaser", x: 1820, y: 1020 }, { type: "chaser", x: 2020, y: 700 }, { type: "shooter", x: 1940, y: 1130 }] },
    { id: "fractured_conduit", name: "FRACTURED CONDUIT", required: true, trigger: { x: 3050, y: 850, w: 540, h: 700 }, gate: "conduit", spawns: [{ type: "chaser", x: 2900, y: 1070 }, { type: "chaser", x: 3120, y: 630 }, { type: "shooter", x: 3240, y: 1060 }, { type: "shooter", x: 3330, y: 690 }] },
    { id: "relay_core", name: "RELAY CORE", required: true, trigger: { x: 4450, y: 800, w: 560, h: 900 }, gate: null, spawns: [{ type: "chaser", x: 4320, y: 590 }, { type: "chaser", x: 4440, y: 1070 }, { type: "shooter", x: 4560, y: 520 }, { type: "shooter", x: 4610, y: 1120 }, { type: "sentinel", x: 4670, y: 800 }] },
    { id: "side_cache", name: "SIDE CACHE", required: false, trigger: { x: 3440, y: 320, w: 480, h: 360 }, gate: null, spawns: [{ type: "chaser", x: 3310, y: 330 }, { type: "chaser", x: 3500, y: 470 }, { type: "shooter", x: 3630, y: 300 }] }
  ]);
  const ASSETS = Object.freeze({
    floor: { key: "ah-relay-floor", url: "assets/world_exploration/relay_fringe_01/relay_floor.webp" }, relayIdle: { key: "ah-relay-node-idle", url: "assets/world_exploration/relay_fringe_01/dead_relay_node_idle.webp" }, relayActive: { key: "ah-relay-node-active", url: "assets/world_exploration/relay_fringe_01/dead_relay_node_active.webp" }, entrance: { key: "ah-relay-entrance", url: "assets/world_exploration/relay_fringe_01/relay_breach_entrance.webp" }, pylon: { key: "ah-relay-pylon", url: "assets/world_exploration/relay_fringe_01/relay_fractured_pylon.webp" }, conduit: { key: "ah-relay-conduit", url: "assets/world_exploration/relay_fringe_01/relay_broken_conduit.webp" }, panel: { key: "ah-relay-panel", url: "assets/world_exploration/relay_fringe_01/relay_collapsed_signal_panel.webp" }, sentinelIdle: { key: "ah-relay-sentinel-idle", url: "assets/world_exploration/relay_fringe_01/relay_sentinel_idle.webp" }, sentinelAttack: { key: "ah-relay-sentinel-attack", url: "assets/world_exploration/relay_fringe_01/relay_sentinel_attack.webp" }, sentinelDefeated: { key: "ah-relay-sentinel-defeated", url: "assets/world_exploration/relay_fringe_01/relay_sentinel_defeated.webp" }, boundaryH: { key: "ah-relay-boundary-h", url: "assets/world_exploration/relay_fringe_01/relay_boundary_fractured_h_a.webp" }, boundaryV: { key: "ah-relay-boundary-v", url: "assets/world_exploration/relay_fringe_01/relay_boundary_fractured_v_a.webp" }, player: { key: "ah-relay-player-sheet-v1", url: "assets/dojo/v1/processed/alpha_husky_player_sheet_v1.png" }, chaser: { key: "ah-relay-chaser-sheet-v1", url: "assets/world_exploration/relay_fringe_01/corrupted_husky_chaser_sheet_4x4_256.png" }, loot: { key: "ah-relay-loot-sheet-v1", url: "assets/world_exploration/relay_fringe_01/loot_pickups_sheet_2x2_256.png" }, slash: { key: "ah-relay-slash-sheet-v1", url: "assets/world_exploration/relay_fringe_01/melee_slash_sheet_5x256.png" } });
  // Completion contract marker retained for the RF01 server-sync verifier: rf01-complete-sync-v1-20260413.
  const RF01_BUILD = "rf01-return-choice-v1-20260816";
  const UI_BASE = "assets/world_exploration/relay_fringe_01/ui/";
  const UI_TEX = Object.freeze({
    joy: { key: "ah-rf01-ui-joy", file: "hud_joystick_base.png" },
    core: { key: "ah-rf01-ui-core", file: "hud_control_core_icon.png" },
    ring: { key: "ah-rf01-ui-ring", file: "hud_control_ring_outer.png" },
    btnLg: { key: "ah-rf01-ui-btn-lg", file: "hud_control_button_large.png" },
    btnMd: { key: "ah-rf01-ui-btn-md", file: "hud_control_button_medium.png" },
    btnSm: { key: "ah-rf01-ui-btn-sm", file: "hud_control_button_small.png" },
    atk: { key: "ah-rf01-ui-atk", file: "hud_attack_slash_icon.png" },
    howl: { key: "ah-rf01-ui-howl", file: "hud_howl_signal_icon.png" },
    glow: { key: "ah-rf01-ui-glow", file: "hud_ability_ring_glow.png" },
    beamC: { key: "ah-rf01-ui-beam-c", file: "hud_loot_beam_common.png" },
    beamR: { key: "ah-rf01-ui-beam-r", file: "hud_loot_beam_rare.png" },
    beamE: { key: "ah-rf01-ui-beam-e", file: "hud_loot_beam_epic.png" },
    label: { key: "ah-rf01-ui-label", file: "hud_loot_label_frame.png" },
    flash: { key: "ah-rf01-ui-flash", file: "hud_pickup_flash.png" }
  });
  const STORY_ASSETS = Object.freeze({
    relay7Portrait: { key: "relay7_portrait_comms", url: "assets/world_exploration/relay_fringe_01/relay7/relay7_portrait_comms.png" },
    relay7Panel: { key: "relay7_comms_panel", url: "assets/world_exploration/relay_fringe_01/relay7/hud_relay7_comms_panel.png" },
    signalAlert: { key: "relay7_signal_alert_panel", url: "assets/world_exploration/relay_fringe_01/relay7/hud_signal_alert_panel.png" }
  });
  // Content only. Gameplay hooks and UI presentation remain below.
  const RF01_STORY = Object.freeze({
    intro: Object.freeze({ type: "relay7", lines: ["Can you hear me?", "This sector was supposed to be dead."], duration: 5200 }),
    firstContact: Object.freeze({ type: "relay7", lines: ["That's not normal corruption.", "They're guarding something."], duration: 5000 }),
    signalDetected: Object.freeze({ type: "signalAlert", duration: 2600 }),
    brokenLaneArchive: Object.freeze({ type: "archive", lines: ["RELAY NODE 7", "LINK LOST", "", "LAST SIGNAL:", "DO NOT OPEN THE—"], duration: 6200, after: "archiveResponse" }),
    archiveResponse: Object.freeze({ type: "relay7", lines: ["That record shouldn't still exist.", "Keep moving."], duration: 4200 }),
    signalDrop: Object.freeze({ type: "interruption", lines: ["Signal is breaking up.", "You're close to the core."], duration: 520 }),
    signalDropResponse: Object.freeze({ type: "relay7", lines: ["Signal is breaking up.", "You're close to the core."], duration: 4400 }),
    relayReady: Object.freeze({ type: "relay7", lines: ["If that Relay is alive, something's wrong.", "Don't activate it yet."], duration: 5000 }),
    relayActive: Object.freeze({ type: "sequence", next: "relayActiveSignal", delay: 420 }),
    relayActiveSignal: Object.freeze({ type: "signalAlert", duration: 3400, after: "relayActiveResponse" }),
    relayActiveResponse: Object.freeze({ type: "relay7", lines: ["...No.", "Something answered."], duration: 4200, after: "returnDecision" }),
    returnDecision: Object.freeze({ type: "choice", lines: ["The archive was a warning.", "We opened it anyway.", "", "I can trace the response.", "But the link has to stay open."] }),
    traceSelected: Object.freeze({ type: "relay7", lines: ["Keep moving.", "I'm following it."], duration: 3600 }),
    traceContact: Object.freeze({ type: "relay7", lines: ["That's new.", "It wasn't there before."], duration: 3800 }),
    traceRecovered: Object.freeze({ type: "relay7", lines: ["Got it. It's not coming from this sector.", "It's deeper."], duration: 4400 }),
    burnSelected: Object.freeze({ type: "relay7", lines: ["Do it.", "Cutting the link now."], duration: 3200, after: "burnSever" }),
    burnSever: Object.freeze({ type: "interruption", label: "LINK // SEVERING", duration: 620 }),
    burnSignalReturn: Object.freeze({ type: "relay7", lines: ["That's impossible.", "I killed the link."], duration: 4200 }),
    burnNearExtract: Object.freeze({ type: "relay7", lines: ["Keep moving.", "We're not clear yet."], duration: 3200 }),
    completion: Object.freeze({ type: "completion", lines: ["If you want to know what answered...", "we're going deeper."] })
  });
  const S = { tg: null, apiPost: null, combatProfile: null, resolveCombatProfile: null, threatTier: "standard", dbg: false, trace: null, root: null, canvasParent: null, loading: null, game: null, scene: null, opening: null, closing: false, ready: false, generation: 0, resolveReady: null, rejectReady: null, readyTimer: null, pageStyles: null, navRegistered: false, onClose: null, completedRunIds: new Set(), safeInsets: { top: 0, right: 0, bottom: 0, left: 0 }, hud: null, hudFingerprint: "", radarFingerprint: "", radarAt: 0 };

  const log = (message, detail) => { if (S.dbg) try { console.debug("[RelayFringe] " + message, detail || ""); } catch (_) {} };
  const trace = stage => { try { (S.trace || global.__AH_RELAY_STARTUP_TRACE)?.(stage); } catch (_) {} };
  const isTouch = () => global.__AH_EXPLORATION_FORCE_TOUCH === true || !!global.matchMedia?.("(pointer: coarse)")?.matches;
  const runId = () => global.AlphaSectorCombatConfig?.makeRunId?.("relay_fringe", SECTOR.sectorId) || global.AH_makeRunId?.("relay_fringe", SECTOR.sectorId) || "relay_fringe_" + Date.now();
  const sq = (a, b) => { const x = a.x - b.x, y = a.y - b.y; return x * x + y * y; };
  const inRange = (a, b, range) => sq(a, b) <= range * range;
  const enemyChildren = scene => global.AlphaSectorCombatConfig?.getEnemyChildren?.(scene) || [];
  const setEnemyVelocity = (enemy, x, y) => global.AlphaSectorCombatConfig?.setArcadeVelocity?.(enemy, x, y) === true;

  function injectHudCSS() { if (global.AlphaRf01ProductionHud?.inject) { global.AlphaRf01ProductionHud.inject(); return; } if (document.getElementById("ah-rf01-hud-css")) return; const style = document.createElement("style"); style.id = "ah-rf01-hud-css"; style.textContent = ".ah-rf-hud{position:absolute;inset:0;z-index:8;pointer-events:none}"; document.head.appendChild(style); }
  function addHudImage(scene, key, x, y, size, depth) { if (!scene.textures.exists(key)) return null; return scene.add.image(x, y, key).setDisplaySize(size, size).setScrollFactor(0).setDepth(depth); }
  function destroyLootFx(pickup) { pickup?.ahBeam?.destroy?.(); pickup?.ahLabel?.destroy?.(); pickup?.ahTag?.destroy?.(); }
  function injectCSS() { if (document.getElementById(STYLE_ID)) return; injectHudCSS(); const style = document.createElement("style"); style.id = STYLE_ID; style.textContent = `.ah-exploration-room{position:fixed;inset:0;z-index:12060;display:grid;grid-template-rows:auto minmax(0,1fr);height:100dvh;overflow:hidden;background:#05090d;color:#e8f3ff;font-family:Inter,system-ui,sans-serif;touch-action:none}.ah-exploration-room__header{min-height:56px;display:grid;grid-template-columns:76px minmax(0,1fr) 76px;align-items:center;gap:8px;padding:max(8px,env(safe-area-inset-top)) max(10px,env(safe-area-inset-right)) 8px max(10px,env(safe-area-inset-left));background:linear-gradient(#111a23,#091018);border-bottom:1px solid rgba(101,232,255,.24)}.ah-exploration-room__back{min-height:40px;border:1px solid rgba(151,188,207,.3);border-radius:9px;background:#101a25;color:#dcecff;font:700 11px/1 system-ui;letter-spacing:.08em;text-transform:uppercase}.ah-exploration-room__heading{text-align:center;min-width:0}.ah-exploration-room__heading span{display:block;color:#7f96a8;font-size:9px;font-weight:800;letter-spacing:.17em}.ah-exploration-room__heading strong{display:block;margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:15px;letter-spacing:.04em}.ah-exploration-room__seal{color:#65e8ff;text-align:center;font-size:10px;letter-spacing:.12em}.ah-exploration-room__stage,.ah-exploration-room__canvas{position:relative;min-width:0;min-height:0;overflow:hidden;touch-action:none}.ah-exploration-room__canvas{position:absolute;inset:0}.ah-exploration-room__canvas canvas{display:block!important;width:100%!important;height:100%!important;touch-action:none!important}.ah-exploration-room__loading{position:absolute;inset:0;display:grid;place-items:center;background:radial-gradient(circle at 50% 44%,rgba(26,76,92,.22),transparent 45%),#071019;color:#9cb2c3;font-size:12px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;pointer-events:none}.ah-exploration-room__loading[hidden]{display:none!important}.ah-sector-result{position:absolute;inset:0;z-index:20;display:grid;place-items:center;padding:18px;background:rgba(3,7,11,.72);pointer-events:auto}.ah-sector-result__card{width:min(92vw,380px);padding:22px 18px;border:1px solid rgba(101,232,255,.55);border-radius:15px;background:linear-gradient(160deg,rgba(16,35,45,.98),rgba(7,13,20,.98));box-shadow:0 18px 50px rgba(0,0,0,.55);text-align:center}.ah-sector-result__card--failed{border-color:rgba(255,128,151,.65)}.ah-sector-result__eyebrow{font-size:10px;font-weight:800;letter-spacing:.14em;color:#80a3b6}.ah-sector-result h2{margin:6px 0 4px;font-size:23px}.ah-sector-result p{margin:0;color:#c5d6df;font-size:13px}.ah-sector-result dl{display:grid;grid-template-columns:1fr auto;gap:7px 14px;margin:18px 0;text-align:left;font-size:13px}.ah-sector-result dt{color:#8aa3b2}.ah-sector-result dd{margin:0;color:#effaff;font-weight:800}.ah-sector-result__actions{display:grid;gap:9px}.ah-sector-result button{min-height:43px;border:1px solid rgba(143,245,255,.72);border-radius:9px;background:#174755;color:#e5fdff;font:800 12px system-ui;letter-spacing:.06em}.ah-sector-result button[data-sector-run-action="map"]{border-color:rgba(201,215,230,.38);background:#17212c;color:#dce8f6}`; document.head.appendChild(style); }
  function buildShell() { document.getElementById(ROOT_ID)?.remove(); const root = document.createElement("section"); root.id = ROOT_ID; root.className = "ah-exploration-room ah-exploration-room--pack"; root.setAttribute("role", "dialog"); root.setAttribute("aria-modal", "true"); root.setAttribute("aria-label", SECTOR.displayName); root.innerHTML = `<header class="ah-exploration-room__header"><button type="button" class="ah-exploration-room__back" data-exploration-room-action="back">Back</button><div class="ah-exploration-room__heading"><span>ACTIVE SECTOR</span><strong>RELAY FRINGE 01</strong></div><span class="ah-exploration-room__seal" aria-hidden="true">LOCAL</span></header><div class="ah-exploration-room__stage"><div id="${CANVAS_ID}" class="ah-exploration-room__canvas"></div>${global.AlphaRf01ProductionHud?.markup?.() || '<div class="ah-rf-hud"></div>'}<div class="ah-exploration-room__loading" role="status">Entering Relay Fringe...</div></div>`; document.body.appendChild(root); S.root = root; S.canvasParent = root.querySelector("#" + CANVAS_ID); S.loading = root.querySelector(".ah-exploration-room__loading"); S.hud = global.AlphaRf01ProductionHud?.bind?.(root.querySelector(".ah-rf-hud")) || { root: root.querySelector(".ah-rf-hud"), hpBar: root.querySelector("[data-rf-hud='hp-bar']"), hp: root.querySelector("[data-rf-hud='hp']"), loot: root.querySelector("[data-rf-hud='loot']"), tier: root.querySelector("[data-rf-hud='tier']"), objTitle: root.querySelector("[data-rf-hud='obj-title']"), objDetail: root.querySelector("[data-rf-hud='obj-detail']"), radar: root.querySelector(".ah-rf-hud__radar-canvas") }; }
  function lockPage() { if (S.pageStyles) return; const b = document.body.style, h = document.documentElement.style; S.pageStyles = { bo: b.overflow, bt: b.touchAction, ho: h.overflow, ht: h.touchAction }; b.overflow = h.overflow = "hidden"; b.touchAction = h.touchAction = "none"; }
  function restorePage() { if (!S.pageStyles) return; const s = S.pageStyles, b = document.body.style, h = document.documentElement.style; b.overflow = s.bo; b.touchAction = s.bt; h.overflow = s.ho; h.touchAction = s.ht; S.pageStyles = null; }
  function readInsets() { if (!S.root) return; const p = document.createElement("i"); p.style.cssText = "position:absolute;visibility:hidden;padding:env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left)"; S.root.appendChild(p); const c = getComputedStyle(p); S.safeInsets = { top: parseFloat(c.paddingTop) || 0, right: parseFloat(c.paddingRight) || 0, bottom: parseFloat(c.paddingBottom) || 0, left: parseFloat(c.paddingLeft) || 0 }; p.remove(); }
  function registerNavigation() { if (global.AlphaNav?.push) { global.AlphaNav.push(ROOT_ID, { close: () => void close({ fromNavigation: true }), isOpen, fallback: false }); S.navRegistered = true; } }
  function unregisterNavigation(fromNavigation) { if (!S.navRegistered) return; if (!fromNavigation) try { global.navClose?.(ROOT_ID); } catch (_) {} try { global.navUnregister?.(ROOT_ID); } catch (_) {} S.navRegistered = false; }
  function onResize() { if (!S.game || !S.canvasParent) return; readInsets(); const w = Math.max(1, S.canvasParent.clientWidth || global.innerWidth || 1), h = Math.max(1, S.canvasParent.clientHeight || global.innerHeight || 1); try { S.game.scale.resize(w, h); S.scene?.ahLayout?.(w, h, S.safeInsets); } catch (error) { log("resize", error); } }
  function onVisibility() { const scene = S.scene; if (!scene) return; if (document.hidden) { scene.ahPaused = true; scene.ahResetInput?.(); scene.ahStop?.(); scene.physics.world.pause(); } else if (scene.ahPaused && !S.closing) { scene.ahPaused = false; scene.physics.world.resume(); } }
  function onRootClick(event) { const target = event.target.closest?.("[data-exploration-room-action],[data-sector-run-action]"); const action = target?.getAttribute("data-exploration-room-action") || target?.getAttribute("data-sector-run-action"); if (action === "back" || action === "map") void close(); else if (action === "again" || action === "restart") void startRun(S.scene, true); else if (action === "retry") submitCompletionOnce(S.scene?.ahRun); else if (action === "continue") void continueNextSector(); }
  function canOfferRf02() { try { return global.WorldExploration?.canEnterSector?.("relay_fringe_02") === true; } catch (_) { return false; } }
  function setResultChrome(open) { S.root?.classList.toggle("is-result-open", !!open); const scene = S.scene, a = open ? .12 : 1; if (!scene) return; [scene.ahJoyRing, scene.ahJoyBase, scene.ahJoyThumb, scene.ahAttackRing, scene.ahAttackIcon, scene.ahAttackLabel, scene.ahHowlGlow, scene.ahHowlRing, scene.ahHowlIcon, scene.ahHowlLabel, scene.ahInteractRing, scene.ahInteractLabel].forEach(item => item?.setAlpha?.(a)); }
  async function continueNextSector() { if (!canOfferRf02()) return; const nextId = "relay_fringe_02"; try { await close(); } catch (_) {} try { await global.WorldExploration?.refreshState?.({ force: true }); } catch (_) {} if (global.WorldExploration?.canEnterSector?.("relay_fringe_02") !== true) return; try { if (typeof global.WorldExploration?.enterSector === "function") await global.WorldExploration.enterSector(nextId); else global.WorldExploration?.openSector?.(nextId); } catch (error) { log("continue next sector", error); } }
  function bindEvents() { S.root.addEventListener("click", onRootClick); global.addEventListener("resize", onResize); document.addEventListener("visibilitychange", onVisibility); try { S.tg?.onEvent?.("viewportChanged", onResize); } catch (_) {} }
  function unbindEvents() { S.root?.removeEventListener("click", onRootClick); global.removeEventListener("resize", onResize); document.removeEventListener("visibilitychange", onVisibility); try { S.tg?.offEvent?.("viewportChanged", onResize); } catch (_) {} }

  function makeAnimations(scene) { const dirs = ["down", "up", "right", "left"]; if (scene.textures.exists(ASSETS.player.key)) dirs.forEach((dir, row) => { const idle = "ah-relay-player-" + dir + "-idle", walk = "ah-relay-player-" + dir + "-walk"; if (!scene.anims.exists(idle)) scene.anims.create({ key: idle, frames: [{ key: ASSETS.player.key, frame: row * 4 }], repeat: -1 }); if (!scene.anims.exists(walk)) scene.anims.create({ key: walk, frames: [1, 2, 3, 2].map(n => ({ key: ASSETS.player.key, frame: row * 4 + n })), frameRate: 7, repeat: -1 }); }); dirs.forEach((dir, row) => { const idle = "ah-relay-chaser-" + dir + "-idle", walk = "ah-relay-chaser-" + dir + "-walk"; if (!scene.anims.exists(idle)) scene.anims.create({ key: idle, frames: [{ key: ASSETS.chaser.key, frame: row * 4 }], repeat: -1 }); if (!scene.anims.exists(walk)) scene.anims.create({ key: walk, frames: [1, 2, 3, 2].map(n => ({ key: ASSETS.chaser.key, frame: row * 4 + n })), frameRate: 7, repeat: -1 }); }); if (!scene.anims.exists("ah-relay-slash")) scene.anims.create({ key: "ah-relay-slash", frames: [0, 1, 2, 3, 4].map(frame => ({ key: ASSETS.slash.key, frame })), frameRate: 26, hideOnComplete: true }); }
  function setAnimation(sprite, prefix, direction, moving) { const key = prefix + "-" + (direction || sprite.ahDirection || "down") + (moving ? "-walk" : "-idle"); sprite.ahDirection = direction || sprite.ahDirection || "down"; if (sprite.anims?.currentAnim?.key !== key) sprite.play?.(key); }
  function addCollider(scene, group, x, y, width, height) { const block = scene.add.rectangle(x, y, width, height, 0x000000, 0).setVisible(false); scene.physics.add.existing(block, true); group.add(block); return block; }
  function addArt(scene, asset, x, y, scale, depth, options) { if (!scene.textures.exists(asset.key)) return null; const image = scene.add.image(x, y, asset.key).setScale(scale).setDepth(depth); if (options?.origin) image.setOrigin(options.origin[0], options.origin[1]); if (options?.flipX) image.setFlipX(true); if (options?.rotation) image.setRotation(options.rotation); if (options?.alpha !== undefined) image.setAlpha(options.alpha); return image; }
  function addShadow(scene, owner, size) { const shadow = scene.add.ellipse(owner.x, owner.y + 35, size, Math.max(8, size * .28), 0x000000, .36).setDepth(owner.depth - 1); owner.ahShadow = shadow; return shadow; }
  function addDressing(scene, asset, x, y, scale, depth, options) { const art = addArt(scene, asset, x, y, scale, depth, options); if (art && options?.shadow) scene.add.ellipse(x, y + (options.shadowOffset ?? 20), options.shadow, Math.max(8, options.shadow * .22), 0x020507, .28).setDepth(depth - .5); return art; }
  function addFloorAccent(scene, x, y, width, height, rotation, alpha) { if (!scene.textures.exists(ASSETS.floor.key)) return; scene.add.image(x, y, ASSETS.floor.key).setDisplaySize(width, height).setRotation(rotation).setAlpha(alpha).setTint(0x9ba7a9).setDepth(1); }
  function buildWorld(scene) {
    for (let x = 350; x < WORLD.width; x += 700) for (let y = 350; y < WORLD.height; y += 700) { if (scene.textures.exists(ASSETS.floor.key)) scene.add.image(x, y, ASSETS.floor.key).setScale(.56).setDepth(0); else scene.add.rectangle(x, y, 700, 700, 0x0b151d, 1).setDepth(0); }
    [[430, 1120, 360, 250, -.03, .08], [1900, 820, 420, 280, .025, .07], [3050, 970, 450, 260, -.02, .07], [4320, 830, 360, 300, .02, .06]].forEach(v => addFloorAccent(scene, ...v));
    const blockers = scene.physics.add.staticGroup(); scene.ahBlockers = blockers; addCollider(scene, blockers, WORLD.width / 2, 20, WORLD.width, 40); addCollider(scene, blockers, WORLD.width / 2, WORLD.height - 20, WORLD.width, 40); addCollider(scene, blockers, 20, WORLD.height / 2, 40, WORLD.height); addCollider(scene, blockers, WORLD.width - 20, WORLD.height / 2, 40, WORLD.height);
    [[430, 810, 250, 42], [750, 1430, 310, 44], [1440, 350, 250, 42], [1660, 1280, 330, 44], [2210, 1200, 260, 42], [2460, 540, 240, 42], [3010, 330, 320, 44], [3220, 1300, 280, 44]].forEach(v => {
      addCollider(scene, blockers, v[0], v[1], v[2], v[3]);
      const barrier = addArt(scene, ASSETS.boundaryH, v[0], v[1] + 8, .24, 9, { origin: [.5, .5] });
      barrier?.setDisplaySize(v[2] + 32, Math.max(54, v[3] * 1.45)).setAlpha(.92);
    });
    [[650, 1100, .20], [930, 720, .20], [1800, 1080, .20], [2710, 680, .22], [3160, 1030, .23]].forEach(v => addDressing(scene, ASSETS.pylon, v[0], v[1], v[2], 12, { origin: [.5, .86], shadow: 108 })); [[1260, 800], [2050, 800], [2850, 800]].forEach(v => addArt(scene, ASSETS.boundaryV, v[0], v[1], .58, 10));
    scene.ahExtractVisual = addDressing(scene, ASSETS.entrance, 220, 1370, .22, 12, { origin: [.5, .82], shadow: 142, shadowOffset: 14 });
    scene.ahEntranceSupports = [addDressing(scene, ASSETS.pylon, 62, 1280, .13, 10, { origin: [.5, .86], shadow: 70, alpha: .72 })].filter(Boolean);
    addDressing(scene, ASSETS.conduit, 2140, 940, .25, 11, { shadow: 130 }); scene.ahArchive = addDressing(scene, ASSETS.panel, 2390, 250, .20, 12, { shadow: 112 }); scene.ahArchiveAnchor = { x: 2390, y: 250 };
    [[1570, 580, ASSETS.panel, .14, .04], [2300, 1125, ASSETS.conduit, .14, -.08], [3390, 1195, ASSETS.panel, .15, -.05], [3660, 570, ASSETS.conduit, .14, .06]].forEach(v => addDressing(scene, v[2], v[0], v[1], v[3], 9, { rotation: v[4], flipX: v[0] === 3660, shadow: 82, alpha: .72 }));
    scene.ahExtractAnchor = { x: WORLD.extract.x, y: WORLD.extract.y };
    scene.ahExtractGlow = scene.add.circle(WORLD.extract.x, WORLD.extract.y, 54, 0x65e8ff, .03).setStrokeStyle(2, 0x65e8ff, .14).setDepth(13);
    scene.ahExtractLabel = scene.add.text(WORLD.extract.x, WORLD.extract.y + 62, "BREACH SEALED", { fontFamily: "system-ui", fontSize: "11px", color: "#e8af91", fontStyle: "bold" }).setOrigin(.5).setDepth(16);
    scene.ahGates = {}; [["approach", 1300], ["lane", 2600], ["conduit", 3900]].forEach(([id, x]) => { const gate = addCollider(scene, blockers, x, 800, 38, 1520); gate.ahVisuals = [280, 800, 1320].map(y => addArt(scene, ASSETS.boundaryV, x, y, .34, 15)); scene.ahGates[id] = gate; });
    scene.ahRelayDressing = [addDressing(scene, ASSETS.panel, WORLD.relay.x + 266, WORLD.relay.y - 170, .14, 10, { rotation: .06, flipX: true, shadow: 82, alpha: .68 })].filter(Boolean);
    scene.ahRelaySupports = [addDressing(scene, ASSETS.pylon, WORLD.relay.x - 188, WORLD.relay.y + 120, .16, 12, { origin: [.5, .86], shadow: 88 }), addDressing(scene, ASSETS.pylon, WORLD.relay.x + 190, WORLD.relay.y + 120, .16, 12, { origin: [.5, .86], flipX: true, shadow: 88 })].filter(Boolean); scene.ahNode = addArt(scene, ASSETS.relayIdle, WORLD.relay.x, WORLD.relay.y, .20, 14) || scene.add.rectangle(WORLD.relay.x, WORLD.relay.y, 136, 150, 0x172f39, .96).setStrokeStyle(3, 0x54727f, .95).setDepth(14); scene.ahNodeAnchor = { x: WORLD.relay.x, y: WORLD.relay.y + 66 }; scene.ahNodeLabel = scene.add.text(WORLD.relay.x, WORLD.relay.y - 116, "RELAY // LOCKED", { fontFamily: "system-ui", fontSize: "11px", color: "#b77a6f", fontStyle: "bold", letterSpacing: 1 }).setOrigin(.5).setDepth(16); scene.ahNodeGlow = scene.add.circle(WORLD.relay.x, WORLD.relay.y + 64, 90, 0x65e8ff, .015).setStrokeStyle(2, 0x53636a, .18).setDepth(13);
    scene.ahCache = addArt(scene, ASSETS.panel, 3540, 255, .22, 14) || scene.add.rectangle(3540, 255, 86, 66, 0x25404c, .9).setStrokeStyle(2, 0xe0ac64, .8).setDepth(14); scene.ahCacheGlow = scene.add.circle(3540, 255, 66, 0xe0ac64, .03).setStrokeStyle(2, 0xe0ac64, .34).setDepth(13);
    if (scene.textures.exists(ASSETS.player.key)) { scene.ahPlayer = scene.physics.add.sprite(WORLD.playerSpawn.x, WORLD.playerSpawn.y, ASSETS.player.key, 8).setScale(.32).setDepth(30); scene.ahPlayer.body.setSize(70, 90).setOffset(93, 150); scene.ahPlayer.setCollideWorldBounds(true); } else { scene.ahPlayer = scene.add.rectangle(WORLD.playerSpawn.x, WORLD.playerSpawn.y, 54, 70, 0x65e8ff, .96).setStrokeStyle(3, 0xdffcff, .95).setDepth(30); scene.physics.add.existing(scene.ahPlayer); scene.ahPlayer.body.setSize(54, 70); scene.ahPlayer.body.setCollideWorldBounds(true); }
    addShadow(scene, scene.ahPlayer, 50); setAnimation(scene.ahPlayer, "ah-relay-player", "right", false); scene.physics.add.collider(scene.ahPlayer, blockers); Object.values(scene.ahGates).forEach(gate => scene.physics.add.collider(scene.ahPlayer, gate));
    scene.ahEnemies = scene.physics.add.group(); scene.ahLoot = scene.physics.add.group(); scene.ahProjectiles = scene.physics.add.group(); scene.ahEnemyUi = []; scene.physics.add.collider(scene.ahEnemies, blockers); scene.physics.add.collider(scene.ahEnemies, scene.ahEnemies); scene.physics.add.collider(scene.ahPlayer, scene.ahEnemies); scene.physics.add.overlap(scene.ahPlayer, scene.ahLoot, collectLoot, undefined, scene); scene.physics.add.overlap(scene.ahPlayer, scene.ahProjectiles, hitProjectile, undefined, scene); scene.physics.add.collider(scene.ahProjectiles, blockers, projectileHitSolid, undefined, scene); Object.values(scene.ahGates).forEach(gate => { scene.physics.add.collider(scene.ahEnemies, gate); scene.physics.add.collider(scene.ahProjectiles, gate, projectileHitSolid, undefined, scene); });
    setRelayState(scene, "locked");
  }
  function setRelayState(scene, state) { scene.ahRelayState = state; const ready = state === "available", restored = state === "activated", energized = ready || restored; if (scene.ahNode?.setTexture && scene.textures.exists(restored ? ASSETS.relayActive.key : ASSETS.relayIdle.key)) scene.ahNode.setTexture(restored ? ASSETS.relayActive.key : ASSETS.relayIdle.key); scene.ahNode?.setTint?.(state === "locked" ? 0x7e8a91 : 0xffffff).setAlpha?.(state === "locked" ? .84 : 1); scene.ahRelaySupports?.forEach(support => support.setAlpha(energized ? .94 : .58)); scene.ahNodeGlow?.setAlpha(restored ? .24 : ready ? .16 : .015).setStrokeStyle(2, energized ? 0x65e8ff : 0x53636a, restored ? .9 : ready ? .62 : .18); scene.ahNodeLabel?.setText(state === "locked" ? "RELAY // LOCKED" : restored ? "RELAY // ACTIVE" : "RELAY // READY").setColor(state === "locked" ? "#b77a6f" : "#baf6ff"); scene.tweens.killTweensOf(scene.ahNodeGlow); if (ready) scene.tweens.add({ targets: scene.ahNodeGlow, alpha: .30, scaleX: 1.08, scaleY: 1.08, duration: 750, yoyo: true, repeat: -1 }); if (restored) scene.tweens.add({ targets: [scene.ahNode, scene.ahNodeGlow], scaleX: "+=.04", scaleY: "+=.04", duration: 260, yoyo: true, repeat: 1 }); setExtractState(scene, restored); }
  function setExtractState(scene, unlocked) { scene.ahExtractUnlocked = unlocked === true; scene.ahExtractGlow?.setAlpha(unlocked ? .18 : .03).setStrokeStyle(2, unlocked ? 0x65e8ff : 0x53636a, unlocked ? .76 : .14); scene.ahExtractVisual?.setAlpha(unlocked ? 1 : .66).setTint?.(unlocked ? 0xffffff : 0x8b969a); scene.ahExtractLabel?.setText(unlocked ? "EXTRACTION OPEN" : "BREACH SEALED").setColor(unlocked ? "#baf6ff" : "#b77a6f"); }
  function clearStoryUi(scene, immediate = true) { const story = scene?.ahStory; if (!story) return; Object.values(story.timers || {}).forEach(timer => timer?.remove?.(false)); [story.comms, story.alert, story.archive, story.interruption, story.choice].forEach(overlay => { if (!overlay?.items) return; scene.tweens?.killTweensOf?.(overlay.items); overlay.items.forEach(item => { item?.disableInteractive?.(); if (immediate) item?.destroy?.(); }); }); story.comms = story.alert = story.archive = story.interruption = story.choice = null; story.timers = {}; story.queue = []; }
  function initialiseStoryState(scene, run) { clearStoryUi(scene); scene.ahStory = { runId: run.runId, played: {}, queue: [], timers: {}, comms: null, alert: null, archive: null, interruption: null, choice: null }; }
  function storyIsCurrent(scene, story) { return !!(story && scene?.ahStory === story && scene.ahRun?.runId === story.runId && scene.ahRun?.status !== "completed"); }
  function storyBusy(story) { return !!(story?.comms || story?.alert || story?.archive || story?.interruption || story?.choice); }
  function storyTimer(scene, name, delay, callback) { const story = scene?.ahStory; if (!story) return null; story.timers[name]?.remove?.(false); const timer = scene.time.delayedCall(delay, () => { if (story.timers[name] === timer) delete story.timers[name]; if (storyIsCurrent(scene, story)) callback?.(); }); story.timers[name] = timer; return timer; }
  function storyFinished(scene, overlay, next) { const story = scene?.ahStory; if (!storyIsCurrent(scene, story)) return; next && playStoryBeat(scene, next); drainStoryQueue(scene); }
  function layoutStory(scene, w, h, insets) { const story = scene.ahStory, edge = 12 + (insets?.left || 0), hudBottom = 68 + (insets?.top || 0), comms = story?.comms; if (comms?.panel?.active) { const scale = Math.max(.31, Math.min(.50, (w - edge * 2) / 900)), panelH = (comms.panel.height || 425) * scale, x = w / 2, y = Math.max(hudBottom + 10 + panelH / 2, h * .205); comms.panel.setPosition(x, y).setScale(scale); comms.text?.setPosition(x - 165 * scale, y + 46 * scale).setFontSize(Math.max(11, Math.round(27 * scale))).setWordWrapWidth(570 * scale); }
    const alert = story?.alert; if (alert?.panel?.active) { const scale = Math.max(.34, Math.min(.50, (w - edge * 2) / 900)), panelH = (alert.panel.height || 260) * scale, y = Math.max(hudBottom + 10 + panelH / 2, h * .19); alert.panel.setPosition(w / 2, y).setScale(scale); }
    const archive = story?.archive; if (archive?.items?.length) { const y = Math.max(hudBottom + 14 + 63, h * .18); archive.back?.setPosition(w / 2, y); archive.title?.setPosition(w / 2, y - 52); archive.text?.setPosition(w / 2, y - 32); }
    const interruption = story?.interruption; if (interruption?.text?.active) interruption.text.setPosition(w / 2, hudBottom + 16);
    const choice = story?.choice; if (choice?.panel?.active) { const scale = Math.max(.31, Math.min(.50, (w - edge * 2) / 900)), panelH = (choice.panel.height || 297) * scale, x = w / 2, y = Math.max(hudBottom + 10 + panelH / 2, h * .205), buttonY = y + panelH / 2 + 24, buttonW = Math.max(120, Math.min(156, (w - edge * 2 - 18) / 2)); choice.panel.setPosition(x, y).setScale(scale); choice.text?.setPosition(x - 165 * scale, y + 42 * scale).setFontSize(Math.max(10, Math.round(24 * scale))).setWordWrapWidth(570 * scale); [[choice.traceBack, choice.traceHit, choice.traceLabel, x - buttonW / 2 - 5], [choice.burnBack, choice.burnHit, choice.burnLabel, x + buttonW / 2 + 5]].forEach(([back, hit, label, bx]) => { back?.setPosition(bx, buttonY).setDisplaySize(buttonW, 42); hit?.setPosition(bx, buttonY).setSize(buttonW, 46); label?.setPosition(bx, buttonY); }); }
  }
  function hideRelay7Message(scene, immediate = false) { const story = scene?.ahStory, overlay = story?.comms; if (!overlay?.items) return; story.timers.comms?.remove?.(false); delete story.timers.comms; scene.tweens?.killTweensOf?.(overlay.items); const dispose = () => { if (scene.ahStory === story && story.comms === overlay) story.comms = null; overlay.items.forEach(item => item?.destroy?.()); storyFinished(scene, overlay, overlay.after); }; if (immediate) return dispose(); scene.tweens.add({ targets: overlay.items, alpha: 0, duration: 150, onComplete: dispose }); }
  function showRelay7Message(scene, message) { const story = scene?.ahStory; if (!storyIsCurrent(scene, story) || !message?.lines?.length || !scene.textures.exists(STORY_ASSETS.relay7Panel.key)) return false; const panel = scene.add.image(0, 0, STORY_ASSETS.relay7Panel.key).setScrollFactor(0).setDepth(121).setAlpha(0), text = scene.add.text(0, 0, message.lines.join("\n"), { fontFamily: "system-ui", fontStyle: "bold", color: "#d9fbff", lineSpacing: 3, wordWrap: { width: 220 } }).setOrigin(0, .5).setScrollFactor(0).setDepth(122).setAlpha(0), overlay = { panel, text, items: [panel, text], after: message.after }; story.comms = overlay; layoutStory(scene, scene.scale.width, scene.scale.height, S.safeInsets); scene.tweens.add({ targets: overlay.items, alpha: 1, duration: 180 }); storyTimer(scene, "comms", message.duration || 4800, () => hideRelay7Message(scene)); return true; }
  function hideSignalAlert(scene, immediate = false) { const story = scene?.ahStory, overlay = story?.alert; if (!overlay?.items) return; story.timers.alert?.remove?.(false); delete story.timers.alert; scene.tweens?.killTweensOf?.(overlay.items); const dispose = () => { if (scene.ahStory === story && story.alert === overlay) story.alert = null; overlay.items.forEach(item => item?.destroy?.()); storyFinished(scene, overlay, overlay.after); }; if (immediate) return dispose(); scene.tweens.add({ targets: overlay.items, alpha: 0, duration: 150, onComplete: dispose }); }
  function showSignalAlert(scene, alert) { const story = scene?.ahStory; if (!storyIsCurrent(scene, story) || !scene.textures.exists(STORY_ASSETS.signalAlert.key)) return false; const panel = scene.add.image(0, 0, STORY_ASSETS.signalAlert.key).setScrollFactor(0).setDepth(123).setAlpha(0), overlay = { panel, items: [panel], after: alert.after }; story.alert = overlay; layoutStory(scene, scene.scale.width, scene.scale.height, S.safeInsets); scene.tweens.add({ targets: panel, alpha: 1, duration: 170 }); storyTimer(scene, "alert", alert.duration || 2400, () => hideSignalAlert(scene)); return true; }
  function hideArchive(scene, immediate = false) { const story = scene?.ahStory, overlay = story?.archive; if (!overlay?.items) return; story.timers.archive?.remove?.(false); delete story.timers.archive; scene.tweens?.killTweensOf?.(overlay.items); const dispose = () => { if (scene.ahStory === story && story.archive === overlay) story.archive = null; overlay.items.forEach(item => item?.destroy?.()); storyFinished(scene, overlay, overlay.after); }; if (immediate) return dispose(); scene.tweens.add({ targets: overlay.items, alpha: 0, duration: 150, onComplete: dispose }); }
  function showArchive(scene, archive) { const story = scene?.ahStory; if (!storyIsCurrent(scene, story)) return false; const back = scene.add.rectangle(0, 0, 252, 126, 0x071018, .96).setStrokeStyle(1, 0x7cc4d2, .72).setScrollFactor(0).setDepth(125).setAlpha(0), title = scene.add.text(0, 0, "DAMAGED ARCHIVE", { fontFamily: "system-ui", fontSize: "10px", fontStyle: "bold", color: "#82b8c2", letterSpacing: 1 }).setOrigin(.5, 0).setScrollFactor(0).setDepth(126).setAlpha(0), text = scene.add.text(0, 0, archive.lines.join("\n"), { fontFamily: "monospace", fontSize: "12px", color: "#d9fbff", lineSpacing: 2, align: "center" }).setOrigin(.5, 0).setScrollFactor(0).setDepth(126).setAlpha(0), overlay = { back, title, text, items: [back, title, text], after: archive.after }; story.archive = overlay; layoutStory(scene, scene.scale.width, scene.scale.height, S.safeInsets); scene.tweens.add({ targets: overlay.items, alpha: 1, duration: 140 }); storyTimer(scene, "archive", archive.duration || 6000, () => hideArchive(scene)); return true; }
  function showSignalInterruption(scene, interruption) { const story = scene?.ahStory; if (!storyIsCurrent(scene, story)) return false; const text = scene.add.text(0, 0, interruption.label || "TRANSMISSION // BREAKING", { fontFamily: "monospace", fontSize: "10px", fontStyle: "bold", color: "#baf6ff", backgroundColor: "rgba(5,10,15,.78)", padding: { x: 8, y: 4 }, letterSpacing: 1 }).setOrigin(.5, 0).setScrollFactor(0).setDepth(124).setAlpha(.15), overlay = { text, items: [text], after: interruption.after }; story.interruption = overlay; layoutStory(scene, scene.scale.width, scene.scale.height, S.safeInsets); scene.tweens.add({ targets: text, alpha: .95, duration: 90, yoyo: true, repeat: 2 }); storyTimer(scene, "interruption", interruption.duration || 520, () => { if (story.interruption !== overlay) return; story.interruption = null; text.destroy(); if (overlay.after) playStoryBeat(scene, overlay.after); else if (interruption === RF01_STORY.signalDrop) playStoryBeat(scene, "signalDropResponse"); drainStoryQueue(scene); }); return true; }
  function hideReturnChoice(scene, immediate = false) { const story = scene?.ahStory, overlay = story?.choice; if (!overlay?.items) return; overlay.items.forEach(item => item?.disableInteractive?.()); scene.tweens?.killTweensOf?.(overlay.items); const dispose = () => { if (scene.ahStory === story && story.choice === overlay) story.choice = null; overlay.items.forEach(item => item?.destroy?.()); storyFinished(scene, overlay); }; if (immediate) return dispose(); scene.tweens.add({ targets: overlay.items, alpha: 0, duration: 140, onComplete: dispose }); }
  function selectReturnChoice(scene, choice) { const run = scene?.ahRun, story = scene?.ahStory; if (!run || !storyIsCurrent(scene, story) || run.returnChoice || (choice !== "trace" && choice !== "burn")) return; run.returnChoice = choice; run.returnPhaseStarted = true; if (choice === "burn") run.burnLinkSevered = true; hideReturnChoice(scene, true); updateHud(scene); updateContext(scene); playStoryBeat(scene, choice === "trace" ? "traceSelected" : "burnSelected"); }
  function showReturnChoice(scene, decision) { const story = scene?.ahStory, run = scene?.ahRun; if (!storyIsCurrent(scene, story) || run?.returnChoice || !scene.textures.exists(STORY_ASSETS.relay7Panel.key)) return false; const panel = scene.add.image(0, 0, STORY_ASSETS.relay7Panel.key).setScrollFactor(0).setDepth(121).setAlpha(0), text = scene.add.text(0, 0, decision.lines.join("\n"), { fontFamily: "system-ui", fontStyle: "bold", color: "#d9fbff", lineSpacing: 2, wordWrap: { width: 220 } }).setOrigin(0, .5).setScrollFactor(0).setDepth(122).setAlpha(0), makeButton = (label, colour, action) => { const back = scene.add.rectangle(0, 0, 144, 42, colour, .92).setStrokeStyle(1, 0xbaf6ff, .68).setScrollFactor(0).setDepth(123).setAlpha(0); const hit = scene.add.rectangle(0, 0, 144, 46, 0x000000, .001).setInteractive({ useHandCursor: true }).setScrollFactor(0).setDepth(125).setAlpha(0); const caption = scene.add.text(0, 0, label, { fontFamily: "system-ui", fontSize: "10px", fontStyle: "bold", color: "#e7fdff", letterSpacing: .8 }).setOrigin(.5).setScrollFactor(0).setDepth(124).setAlpha(0); hit.on("pointerdown", (pointer, _x, _y, event) => { event?.stopPropagation?.(); selectReturnChoice(scene, action); }); return { back, hit, caption }; }, trace = makeButton("TRACE SIGNAL", 0x15546a, "trace"), burn = makeButton("BURN LINK", 0x47303b, "burn"), overlay = { panel, text, traceBack: trace.back, traceHit: trace.hit, traceLabel: trace.caption, burnBack: burn.back, burnHit: burn.hit, burnLabel: burn.caption, items: [panel, text, trace.back, trace.hit, trace.caption, burn.back, burn.hit, burn.caption] }; story.choice = overlay; layoutStory(scene, scene.scale.width, scene.scale.height, S.safeInsets); scene.tweens.add({ targets: overlay.items, alpha: 1, duration: 170 }); return true; }
  function drainStoryQueue(scene) { const story = scene?.ahStory; if (!storyIsCurrent(scene, story) || storyBusy(story)) return; const id = story.queue.shift(), beat = RF01_STORY[id]; if (!beat) return; if (beat.type === "relay7") showRelay7Message(scene, beat); else if (beat.type === "signalAlert") showSignalAlert(scene, beat); else if (beat.type === "archive") showArchive(scene, beat); else if (beat.type === "interruption") showSignalInterruption(scene, beat); else if (beat.type === "choice") showReturnChoice(scene, beat); else if (beat.type === "sequence") storyTimer(scene, "sequence_" + id, beat.delay || 0, () => playStoryBeat(scene, beat.next)); else drainStoryQueue(scene); }
  function playStoryBeat(scene, id) { const story = scene?.ahStory, beat = RF01_STORY[id]; if (!storyIsCurrent(scene, story) || !beat || story.played[id]) return; story.played[id] = true; story.queue.push(id); drainStoryQueue(scene); }
  function openGate(scene, id) { const gate = scene.ahGates?.[id]; if (!gate?.body?.enable) return; gate.body.enable = false; gate.setAlpha(0); gate.ahVisuals?.forEach(visual => scene.tweens.add({ targets: visual, alpha: 0, duration: 250 })); }

  function createControls(scene) { scene.ahMove = new global.Phaser.Math.Vector2(); scene.ahJoy = { x: 0, y: 0 }; scene.ahJoyPointer = null; scene.ahAttackPointer = null; scene.ahHowlPointer = null; scene.ahInteractPointer = null; scene.ahContext = null; scene.ahTouch = isTouch(); scene.ahPaused = false; scene.ahJoyRing = addHudImage(scene, UI_TEX.ring.key, 88, 88, 112, 99); scene.ahJoyBase = addHudImage(scene, UI_TEX.joy.key, 88, 88, 96, 100) || scene.add.circle(88, 88, 54, 0x0b1620, .75).setStrokeStyle(2, 0x65e8ff, .5).setScrollFactor(0).setDepth(100); scene.ahJoyThumb = addHudImage(scene, UI_TEX.core.key, 88, 88, 36, 101) || scene.add.circle(88, 88, 25, 0x65e8ff, .3).setStrokeStyle(2, 0xbaf6ff, .75).setScrollFactor(0).setDepth(101); scene.ahJoyHit = scene.add.zone(88, 88, 160, 160).setInteractive().setScrollFactor(0).setDepth(102); scene.ahAttackRing = addHudImage(scene, UI_TEX.btnLg.key, 0, 0, 78, 100) || scene.add.circle(0, 0, 40, 0x3b122d, .9).setStrokeStyle(2, 0xff70bd, .94).setScrollFactor(0).setDepth(100); scene.ahAttackIcon = addHudImage(scene, UI_TEX.atk.key, 0, 0, 32, 101); scene.ahAttackLabel = scene.add.text(0, 0, "ATK", { fontFamily: "system-ui", fontSize: "9px", color: "#d8fbff", fontStyle: "bold" }).setOrigin(.5, -1.7).setScrollFactor(0).setDepth(101); scene.ahAttackHit = scene.add.zone(0, 0, 96, 96).setInteractive().setScrollFactor(0).setDepth(102); scene.ahHowlGlow = addHudImage(scene, UI_TEX.glow.key, 0, 0, 64, 102); scene.ahHowlRing = addHudImage(scene, UI_TEX.btnMd.key, 0, 0, 56, 103) || scene.add.circle(0, 0, 32, 0x123741, .92).setStrokeStyle(2, 0xbaf6ff, .94).setScrollFactor(0).setDepth(103); scene.ahHowlIcon = addHudImage(scene, UI_TEX.howl.key, 0, 0, 25, 104); scene.ahHowlLabel = scene.add.text(0, 0, "HOWL", { fontFamily: "system-ui", fontSize: "8px", color: "#e8fdff", fontStyle: "bold", align: "center" }).setOrigin(.5, -2.1).setScrollFactor(0).setDepth(104); scene.ahHowlHit = scene.add.zone(0, 0, 84, 84).setInteractive().setScrollFactor(0).setDepth(105); scene.ahInteractRing = addHudImage(scene, UI_TEX.btnSm.key, 0, 0, 46, 106) || scene.add.circle(0, 0, 36, 0x17606c, .94).setStrokeStyle(2, 0xb7fbff, .96).setScrollFactor(0).setDepth(106); scene.ahInteractRing.setVisible(false); scene.ahInteractLabel = scene.add.text(0, 0, "ACTIVATE", { fontFamily: "system-ui", fontSize: "8px", color: "#e4feff", fontStyle: "bold" }).setOrigin(.5).setScrollFactor(0).setDepth(107).setVisible(false); scene.ahInteractHit = scene.add.zone(0, 0, 88, 88).setInteractive().setScrollFactor(0).setDepth(108).setVisible(false); scene.ahInteractHit.input.enabled = false; scene.ahPrompt = scene.add.text(0, 0, "", { fontFamily: "system-ui", fontSize: "12px", color: "#a9edf7", fontStyle: "bold", align: "center" }).setOrigin(.5, 0).setScrollFactor(0).setDepth(102); scene.ahMessage = scene.add.text(0, 0, "", { fontFamily: "system-ui", fontSize: "13px", color: "#d8fbff", fontStyle: "bold", align: "center", backgroundColor: "rgba(5,10,15,.70)", padding: { x: 10, y: 6 } }).setOrigin(.5).setScrollFactor(0).setDepth(103).setVisible(false); scene.ahHud = scene.add.text(14, 16, "", { fontFamily: "system-ui", fontSize: "11px", color: "#dff8ff", fontStyle: "bold", backgroundColor: "rgba(5,10,15,.68)", padding: { x: 8, y: 6 } }).setScrollFactor(0).setDepth(150); scene.ahTargetRing = scene.add.arc(0, 0, 19, 205, 335, false, 0x000000, 0).setStrokeStyle(2, 0xa84e59, .82).setDepth(22).setVisible(false);
    scene.ahOnJoyDown = p => { if (scene.ahJoyPointer === null && scene.input.enabled) { scene.ahJoyPointer = p.id; updateJoy(scene, p); } }; scene.ahOnAttackDown = p => { if (scene.ahAttackPointer === null && scene.input.enabled) { scene.ahAttackPointer = p.id; playerAttack(scene); } }; scene.ahOnHowlDown = p => { if (scene.ahHowlPointer === null && scene.input.enabled) { scene.ahHowlPointer = p.id; tryHowlBurst(scene); } }; scene.ahOnInteractDown = p => { if (scene.ahInteractPointer === null && scene.input.enabled) { scene.ahInteractPointer = p.id; interact(scene); } }; scene.ahOnMove = p => { if (p.id === scene.ahJoyPointer) updateJoy(scene, p); }; scene.ahOnUp = p => { if (p.id === scene.ahJoyPointer) resetJoy(scene); if (p.id === scene.ahAttackPointer) scene.ahAttackPointer = null; if (p.id === scene.ahHowlPointer) scene.ahHowlPointer = null; if (p.id === scene.ahInteractPointer) scene.ahInteractPointer = null; }; scene.ahOnGameOut = () => scene.ahResetInput(); scene.ahJoyHit.on("pointerdown", scene.ahOnJoyDown); scene.ahAttackHit.on("pointerdown", scene.ahOnAttackDown); scene.ahHowlHit.on("pointerdown", scene.ahOnHowlDown); scene.ahInteractHit.on("pointerdown", scene.ahOnInteractDown); scene.input.on("pointermove", scene.ahOnMove); scene.input.on("pointerup", scene.ahOnUp); scene.input.on("gameout", scene.ahOnGameOut); scene.ahResetInput = () => { resetJoy(scene); scene.ahAttackPointer = scene.ahHowlPointer = scene.ahInteractPointer = null; }; scene.ahStop = () => { scene.ahPlayer?.setVelocity(0, 0); setAnimation(scene.ahPlayer, "ah-relay-player", scene.ahPlayer?.ahDirection, false); }; scene.ahLayout = (w, h, insets) => layout(scene, w, h, insets); layout(scene, scene.scale.width, scene.scale.height, S.safeInsets); }
  function layout(scene, w, h, insets) { global.AlphaSectorCombatConfig?.applyCameraFraming?.(scene, w, h); const compact = w < 430, joyX = Math.max(72, insets.left + (compact ? 72 : 88)), y = h - Math.max(76, insets.bottom + (compact ? 76 : 90)), attackX = w - Math.max(64, insets.right + (compact ? 64 : 78)), interactX = Math.max(joyX + 112, attackX - (compact ? 104 : 116)), howlY = y - (compact ? 76 : 84); [scene.ahJoyRing, scene.ahJoyBase, scene.ahJoyHit].forEach(item => item?.setPosition(joyX, y)); if (scene.ahJoyPointer === null) scene.ahJoyThumb.setPosition(joyX, y); [scene.ahAttackRing, scene.ahAttackIcon, scene.ahAttackLabel, scene.ahAttackHit].forEach(item => item?.setPosition(attackX, y)); [scene.ahHowlGlow, scene.ahHowlRing, scene.ahHowlIcon, scene.ahHowlLabel, scene.ahHowlHit].forEach(item => item?.setPosition(attackX, howlY)); [scene.ahInteractRing, scene.ahInteractLabel, scene.ahInteractHit].forEach(item => item?.setPosition(interactX, y)); scene.ahPrompt.setPosition(w / 2, Math.max(88, insets.top + 88)).setVisible(false); scene.ahMessage.setPosition(w / 2, Math.max(92, insets.top + 92)); scene.ahHud.setPosition(Math.max(10, insets.left + 10), Math.max(10, insets.top + 10)).setVisible(false); layoutStory(scene, w, h, insets); }
  function updateJoy(scene, p) { let x = p.x - scene.ahJoyBase.x, y = p.y - scene.ahJoyBase.y, d = Math.hypot(x, y); if (d > 48) { x *= 48 / d; y *= 48 / d; } scene.ahJoyThumb.setPosition(scene.ahJoyBase.x + x, scene.ahJoyBase.y + y); if (d < 11) { scene.ahJoy.x = scene.ahJoy.y = 0; return; } d = Math.hypot(x, y) || 1; scene.ahJoy.x = x / d; scene.ahJoy.y = y / d; }
  function resetJoy(scene) { scene.ahJoyPointer = null; scene.ahJoy.x = scene.ahJoy.y = 0; scene.ahJoyThumb?.setPosition(scene.ahJoyBase.x, scene.ahJoyBase.y); }
  function createKeyboard(scene) { if (!scene.input.keyboard) return; scene.ahCursors = scene.input.keyboard.createCursorKeys(); scene.ahKeys = scene.input.keyboard.addKeys({ up: global.Phaser.Input.Keyboard.KeyCodes.W, left: global.Phaser.Input.Keyboard.KeyCodes.A, down: global.Phaser.Input.Keyboard.KeyCodes.S, right: global.Phaser.Input.Keyboard.KeyCodes.D, interact: global.Phaser.Input.Keyboard.KeyCodes.E, attack: global.Phaser.Input.Keyboard.KeyCodes.F, howl: global.Phaser.Input.Keyboard.KeyCodes.Q, space: global.Phaser.Input.Keyboard.KeyCodes.SPACE, escape: global.Phaser.Input.Keyboard.KeyCodes.ESC }); }

  function makeRunState() { const combatSnapshot = global.AlphaSectorCombatConfig?.makeRunCombatSnapshot?.(S.combatProfile), threatSnapshot = global.AlphaSectorCombatConfig?.makeRunThreatSnapshot?.(S.threatTier); if (!combatSnapshot || !threatSnapshot) return null; global.AlphaSectorCombatConfig?.logActionCombatProfile?.(SECTOR.sectorId, S.combatProfile, combatSnapshot); return { runId: runId(), sectorId: SECTOR.sectorId, runProfileVersion: SECTOR.runProfileVersion, combatSnapshot, threatTier: threatSnapshot.id, threatSnapshot, status: "active", startedAt: Date.now(), duration: 0, playerHP: combatSnapshot.maxHp, chasersKilled: 0, shootersKilled: 0, sentinelKilled: 0, mandatoryEnemiesKilled: 0, optionalEnemiesKilled: 0, damageTaken: 0, howlUses: 0, cacheOpened: false, expEarned: 0, bonesEarned: 0, scrapEarned: 0, equipmentDrops: 0, relayActivated: false, returnChoice: null, returnPhaseStarted: false, returnNarrativeResolved: false, traceContactTriggered: false, traceContactActive: false, traceContactResolved: false, traceSignalRecovered: false, burnLinkSevered: false, burnSignalReturned: false, burnNearExtractPlayed: false, encounters: Object.fromEntries(ENCOUNTERS.map(e => [e.id, { activated: false, cleared: false, remaining: e.spawns.length }])) }; }
  function isCombatEnabled(run) { return !!(run && (run.status === "active" || (run.status === "cleared" && run.relayActivated === true && run.returnChoice === "trace" && run.traceContactActive === true && run.traceContactResolved !== true))); }
  function spawnTraceReturnContact(scene) { const run = scene?.ahRun, player = scene?.ahPlayer; if (!run || !player || run.traceContactTriggered) return; const candidates = [{ type: "chaser", x: 3120, y: 630 }, { type: "chaser", x: 3240, y: 1060 }]; const spawn = candidates.sort((a, b) => sq(player, b) - sq(player, a))[0]; run.traceContactTriggered = true; run.traceContactActive = true; const enemy = spawnEnemy(scene, spawn, { id: "trace_return_contact", required: false }); if (!enemy) { run.traceContactActive = false; run.traceContactResolved = run.returnNarrativeResolved = true; return; } enemy.ahCombat.returnTrace = true; showMessage(scene, "TRACE CONTACT"); playStoryBeat(scene, "traceContact"); updateHud(scene); }
  function resolveTraceReturnContact(scene) { const run = scene?.ahRun; if (!run || run.traceContactResolved) return; run.traceContactActive = false; run.traceContactResolved = true; run.traceSignalRecovered = true; run.returnNarrativeResolved = true; showMessage(scene, "SIGNAL FRAGMENT RECOVERED"); playStoryBeat(scene, "traceRecovered"); updateHud(scene); updateContext(scene); }
  function updateReturnPhase(scene) { const run = scene?.ahRun, p = scene?.ahPlayer; if (!run?.relayActivated || !run.returnChoice || run.status === "completed" || run.status === "failed" || !p?.active) return; const inReturnConduit = p.x <= 3400 && p.x >= 2600; if (run.returnChoice === "trace" && !run.traceContactTriggered && inReturnConduit && run.encounters.fractured_conduit?.cleared) spawnTraceReturnContact(scene); if (run.returnChoice === "burn" && !run.burnSignalReturned && inReturnConduit && run.encounters.fractured_conduit?.cleared) { run.burnSignalReturned = true; run.returnNarrativeResolved = true; scene.ahSignalPing = { x: Math.min(WORLD.width - 80, p.x + 420), y: Math.max(80, Math.min(WORLD.height - 80, p.y)), until: (scene.time?.now || Date.now()) + 4200 }; showMessage(scene, "SIGNAL CONTACT"); playStoryBeat(scene, "burnSignalReturn"); updateHud(scene); } if (run.returnChoice === "burn" && run.burnSignalReturned && !run.burnNearExtractPlayed && p.x <= 820) { run.burnNearExtractPlayed = true; playStoryBeat(scene, "burnNearExtract"); } }
  function clearRunObjects(scene) { scene.ahSignalPing = null; for (const enemy of enemyChildren(scene)) { enemy.ahRig?.forEach(item => item.destroy?.()); enemy.ahShadow?.destroy?.(); enemy.ahVisual?.destroy?.(); enemy.ahTelegraph?.destroy?.(); } scene.ahEnemies?.clear(true, true); for (const pickup of scene.ahLoot?.getChildren?.() || []) destroyLootFx(pickup); scene.ahLoot?.clear(true, true); scene.ahProjectiles?.clear(true, true); scene.ahEnemyUi?.forEach(item => item.destroy?.()); scene.ahEnemyUi = []; scene.ahTargetRing?.setVisible(false); scene.ahSentinelVisual?.destroy?.(); scene.ahSentinelVisual = null; removeResultPanel(); }
  async function startRun(scene, refreshProfile = false) { if (!scene || S.closing) return; if (refreshProfile) try { const profile = await S.resolveCombatProfile?.(); const normalized = global.AlphaSectorCombatConfig?.normalizeCombatProfile?.(profile); if (!normalized) throw new Error("invalid combat profile"); S.combatProfile = normalized; } catch (_) { showMessage(scene, "Unable to load combat profile - Retry"); return; } const nextRun = makeRunState(); if (!nextRun) { showMessage(scene, "Unable to load combat profile - Retry"); return; } global.AlphaSectorCombatConfig?.clearHowlBurstState?.(scene); clearRunObjects(scene); S.hudFingerprint = ""; S.radarFingerprint = ""; S.radarAt = 0; if (S.hud) { S.hud.fingerprint = ""; S.hud.howlFingerprint = ""; S.hud.radarFingerprint = ""; S.hud.geomDrawn = false; } global.AlphaRf01ProductionHud?.setPrompt?.(S.hud, ""); scene.input.enabled = true; scene.ahRun = nextRun; initialiseStoryState(scene, nextRun); const seal = S.root?.querySelector?.(".ah-exploration-room__seal"); if (seal) seal.textContent = nextRun.threatSnapshot.label; scene.ahPlayerControl = true; scene.ahNextAttackAt = 0; scene.ahNextHowlAt = 0; scene.ahPlayer.enableBody(true, WORLD.playerSpawn.x, WORLD.playerSpawn.y, true, true).setAlpha(1).clearTint(); scene.ahPlayer.setVelocity(0, 0); scene.ahPlayer.ahShadow?.setVisible(true); setAnimation(scene.ahPlayer, "ah-relay-player", "right", false); Object.values(scene.ahGates || {}).forEach(gate => { gate.body.enable = true; gate.setAlpha(1); gate.ahVisuals?.forEach(visual => visual?.setAlpha(1)); }); setRelayState(scene, "locked"); setExtractState(scene, false); scene.ahCache.setTint?.(0xffffff).setAlpha?.(1); scene.ahCacheGlow.setAlpha(.03); updateHud(scene); setContext(scene, null); showMessage(scene, "RELAY FRINGE — MOVE EAST"); storyTimer(scene, "intro", 700, () => playStoryBeat(scene, "intro")); updateRadar(scene, true); }
  function activateEncounter(scene, encounter) { const state = scene.ahRun?.encounters?.[encounter.id]; if (!state || state.activated || scene.ahRun.status !== "active") return; state.activated = true; encounter.spawns.forEach(spawn => spawnEnemy(scene, spawn, encounter)); showMessage(scene, encounter.id === "relay_core" ? "RELAY CORE // ELITE SIGNAL ACTIVE" : encounter.name + " // THREATS ACTIVE"); if (encounter.id === "signal_approach") playStoryBeat(scene, "firstContact"); if (encounter.id === "relay_core") playStoryBeat(scene, "signalDrop"); updateHud(scene); }
  function activateEncounters(scene) { const p = scene.ahPlayer, run = scene.ahRun; if (!p?.active || run?.status !== "active") return; for (const encounter of ENCOUNTERS) { const state = run.encounters[encounter.id], t = encounter.trigger; if (!state.activated && p.x >= t.x - t.w / 2 && p.x <= t.x + t.w / 2 && p.y >= t.y - t.h / 2 && p.y <= t.y + t.h / 2) activateEncounter(scene, encounter); } }
  function spawnEnemy(scene, spawn, encounter) { if (spawn.type === "sentinel") return spawnSentinel(scene, spawn, encounter); const enemy = scene.physics.add.sprite(spawn.x, spawn.y, ASSETS.chaser.key, 0).setScale(spawn.type === "shooter" ? .27 : .25).setDepth(25).setCollideWorldBounds(true), base = spawn.type === "shooter" ? { maxHp: SHOOTER.hp, damage: SHOOTER.damage, cooldownMs: SHOOTER.cooldownMs } : { maxHp: COMBAT.enemyMaxHp, damage: COMBAT.enemyDamage, cooldownMs: COMBAT.enemyAttackCooldownMs }, tuning = global.AlphaSectorCombatConfig?.scaleEnemyCombat?.(base, scene.ahRun?.threatSnapshot) || base; enemy.body.setSize(78, 80).setOffset(89, 142); enemy.ahCombat = { type: spawn.type, encounterId: encounter.id, required: encounter.required, hp: tuning.maxHp, maxHp: tuning.maxHp, damage: tuning.damage, cooldownMs: tuning.cooldownMs, nextAttackAt: 0, dead: false }; setAnimation(enemy, "ah-relay-chaser", "down", false); if (spawn.type === "shooter") makeShooterRig(scene, enemy); addShadow(scene, enemy, spawn.type === "shooter" ? 44 : 40); makeEnemyUi(scene, enemy, spawn.type === "shooter" ? "CORRUPTED SHOOTER" : "CORRUPTED CHASER", spawn.type === "shooter" ? 0xad505a : 0xa84e59); scene.ahEnemies.add(enemy); return enemy; }
  function makeShooterRig(scene, enemy) { const glow = scene.add.circle(enemy.x + 16, enemy.y + 3, 13, 0x762c37, .16).setStrokeStyle(2, 0xaf535d, .62).setDepth(27); const rig = scene.add.rectangle(enemy.x + 20, enemy.y + 5, 24, 8, 0x271216, .96).setStrokeStyle(1, 0xaf535d, .72).setDepth(28); enemy.ahRig = [glow, rig]; }
  function spawnSentinel(scene, spawn, encounter) { const body = scene.add.rectangle(spawn.x, spawn.y, 64, 64, 0x4e2029, .55).setStrokeStyle(3, 0xa94b56, .76).setDepth(27), sentinelBase = { maxHp: SENTINEL.hp, damage: SENTINEL.damage, recoveryMs: SENTINEL.recoveryMs }, tuning = global.AlphaSectorCombatConfig?.scaleEnemyCombat?.(sentinelBase, scene.ahRun?.threatSnapshot) || sentinelBase; scene.physics.add.existing(body); body.body.setSize(58, 58); body.body.setCollideWorldBounds(true); body.ahCombat = { type: "sentinel", encounterId: encounter.id, required: true, hp: tuning.maxHp, maxHp: tuning.maxHp, damage: tuning.damage, recoveryMs: tuning.recoveryMs, nextAttackAt: 0, dead: false, state: "idle", stateUntil: scene.time.now + 800, hitThisLunge: false, howlKnockbackImmune: true }; body.ahVisual = addArt(scene, ASSETS.sentinelIdle, spawn.x, spawn.y + 29, .14, 27, { origin: [.5, .92] }); body.setVisible(!body.ahVisual); addShadow(scene, body, 58); makeEnemyUi(scene, body, "RELAY SENTINEL", 0xad505a); scene.ahEnemies.add(body); scene.physics.add.collider(body, scene.ahBlockers); scene.ahSentinelVisual = body.ahVisual; }
  function makeEnemyUi(scene, enemy, labelText, colour) { const back = scene.add.rectangle(enemy.x, enemy.y - 42, 32, 5, 0x090d10, .58).setDepth(40), bar = scene.add.rectangle(enemy.x, enemy.y - 42, 30, 2, colour, .72).setDepth(41), label = scene.add.text(enemy.x, enemy.y - 57, labelText, { fontFamily: "system-ui", fontSize: "7px", color: "#" + colour.toString(16).padStart(6, "0"), fontStyle: "bold", letterSpacing: .45, backgroundColor: "rgba(5,9,12,.72)", padding: { x: 2, y: 1 } }).setOrigin(.5).setDepth(40); label.setAlpha?.(0); enemy.ahUi = [back, bar, label]; scene.ahEnemyUi.push(...enemy.ahUi); }
  function setEnemyUiFocus(enemy, focused) { const [back, bar, label] = enemy?.ahUi || []; back?.setAlpha(focused ? .86 : .46); bar?.setAlpha(focused ? .98 : .62); label?.setAlpha(focused ? .96 : 0); }
  function updateEnemyUi(enemy) { if (!enemy?.active || !enemy.ahCombat) return; const ratio = Math.max(0, enemy.ahCombat.hp / enemy.ahCombat.maxHp), [back, bar, label] = enemy.ahUi || []; back?.setPosition(enemy.x, enemy.y - 42); bar?.setPosition(enemy.x - 15 + 15 * ratio, enemy.y - 42).setDisplaySize(30 * ratio, 2); label?.setPosition(enemy.x, enemy.y - 57); enemy.ahShadow?.setPosition(enemy.x, enemy.y + (enemy.ahCombat.type === "sentinel" ? 31 : 35)); enemy.ahRig?.[0]?.setPosition(enemy.x + 16, enemy.y + 3); enemy.ahRig?.[1]?.setPosition(enemy.x + 20, enemy.y + 5); if (enemy.ahVisual) enemy.ahVisual.setPosition(enemy.x, enemy.y + 29); }
  function nearestEnemy(scene) { const player = scene.ahPlayer; if (!player?.active) return null; let closest = null, distanceSq = Infinity; for (const enemy of enemyChildren(scene)) { if (!enemy?.active || enemy.ahCombat?.dead) continue; const d = sq(player, enemy); if (d < distanceSq) { closest = enemy; distanceSq = d; } } return closest ? { enemy: closest, distanceSq } : null; }
  function objectiveCopy(run) { const local = ENCOUNTERS.find(e => run.encounters?.[e.id]?.activated && !run.encounters[e.id].cleared); if (run.status === "completed") return { title: "SECTOR COMPLETE", detail: "Signal secured" }; if (run.relayActivated && !Object.prototype.hasOwnProperty.call(run, "returnChoice")) return { title: "RETURN TO THE BREACH", detail: "Breach route open" }; if (run.relayActivated && !run.returnChoice) return { title: "SIGNAL RESPONSE DETECTED", detail: "Relay-7 link active" }; if (run.returnChoice === "trace" && run.traceContactActive) return { title: "RESOLVE TRACE CONTACT", detail: "Anomaly on return route" }; if (run.returnChoice === "trace" && !run.returnNarrativeResolved) return { title: "RETURN TO THE BREACH", detail: "Tracing the response" }; if (run.returnChoice === "burn" && !run.returnNarrativeResolved) return { title: "RETURN TO THE BREACH", detail: "Signal link severed" }; if (run.relayActivated) return { title: "RETURN TO THE BREACH", detail: "Breach route open" }; if (run.status === "cleared") return { title: "AREA SECURED", detail: "Relay ready — move to core" }; if (local) return { title: local.id === "relay_core" ? "SECURE RELAY CORE" : local.name, detail: local.id === "relay_core" ? "Clear the final threat" : "Threats remaining: " + run.encounters[local.id].remaining }; return { title: "REACH RELAY APPROACH", detail: "Follow the fractured signal east" }; }
  function updateHud(scene) { const run = scene.ahRun; if (!run) return; const objective = objectiveCopy(run); global.AlphaRf01ProductionHud?.update?.(S.hud, { hp: run.playerHP, maxHp: run.combatSnapshot.maxHp, title: objective.title, detail: objective.detail, exp: run.expEarned, bones: run.bonesEarned, scrap: run.scrapEarned, gear: run.equipmentDrops, tier: run.threatSnapshot?.label || "STANDARD", status: run.status, relayActivated: run.relayActivated === true, level: run.combatSnapshot?.source?.level || 0, x: scene.ahPlayer?.x || 0, y: scene.ahPlayer?.y || 0 }); updateRadar(scene, false); }
  function updateRadar(scene, force) { const run = scene.ahRun, player = scene.ahPlayer, now = scene.time?.now || Date.now(); if (!run || !player) return; if (scene.ahSignalPing && scene.ahSignalPing.until <= now) scene.ahSignalPing = null; const enemies = enemyChildren(scene).filter(enemy => enemy?.active && !enemy.ahCombat?.dead).sort((a, b) => sq(player, a) - sq(player, b)).slice(0, 4).map(enemy => ({ x: enemy.x, y: enemy.y })); if (force && S.hud) S.hud.radarFingerprint = ""; global.AlphaRf01ProductionHud?.updateMinimap?.(S.hud, { x: player.x, y: player.y, facing: player.ahDirection || "right", status: run.status, relayActivated: run.relayActivated === true, kills: run.mandatoryEnemiesKilled, encounters: run.encounters, enemies, signalPing: scene.ahSignalPing }, WORLD, ENCOUNTERS, now); }
  function showMessage(scene, text) { if (global.AlphaRf01ProductionHud && S.hud) { scene.ahMessageTimer?.remove?.(false); scene.ahMessage?.setVisible(false); return; } scene.ahMessageTimer?.remove?.(false); scene.ahMessage.setText(text).setVisible(true); scene.ahMessageTimer = scene.time.delayedCall(1500, () => scene.ahMessage?.setVisible(false)); }
  function setContext(scene, context) { if (scene.ahContext === context) return; scene.ahContext = context; const visible = !!context && scene.ahTouch && scene.ahRun?.status !== "failed" && scene.ahRun?.status !== "completed"; scene.ahInteractRing.setVisible(visible); scene.ahInteractLabel.setVisible(visible && !global.AlphaRf01ProductionHud); scene.ahInteractHit.setVisible(visible); if (scene.ahInteractHit.input) scene.ahInteractHit.input.enabled = visible; const label = context === "relay" ? "ACTIVATE" : context === "extract" ? "EXIT" : context === "archive" || context === "cache" ? "ACCESS" : ""; scene.ahInteractLabel.setText(label); const prompt = context === "relay" ? (scene.ahTouch ? "ACTIVATE" : "ACTIVATE — E") : context === "extract" ? (scene.ahTouch ? "EXIT" : "EXIT — E") : context === "archive" ? (scene.ahTouch ? "READ ARCHIVE" : "READ ARCHIVE — E") : context === "cache" ? (scene.ahTouch ? "ACCESS" : "ACCESS — E") : ""; scene.ahPrompt.setText(""); global.AlphaRf01ProductionHud?.setPrompt?.(S.hud, prompt); }
  function updateContext(scene) { const run = scene.ahRun, p = scene.ahPlayer; if (!run || !p?.active || run.status === "failed" || run.status === "completed") return setContext(scene, null); if (run.relayActivated && inRange(p, scene.ahExtractAnchor, 124)) { if (run.returnChoice && run.returnNarrativeResolved) return setContext(scene, "extract"); global.AlphaRf01ProductionHud?.setPrompt?.(S.hud, run.returnChoice ? "RESOLVE THE SIGNAL" : "MAKE SIGNAL DECISION"); return setContext(scene, null); } if (run.status === "cleared" && !run.relayActivated && inRange(p, scene.ahNodeAnchor, 116)) return setContext(scene, "relay"); if (!scene.ahStory?.played?.brokenLaneArchive && run.encounters.broken_lane.cleared && inRange(p, scene.ahArchiveAnchor, 104)) return setContext(scene, "archive"); const cacheState = run.encounters.side_cache; if (!run.cacheOpened && cacheState.activated && cacheState.cleared && inRange(p, scene.ahCache, 104)) return setContext(scene, "cache"); global.AlphaRf01ProductionHud?.setPrompt?.(S.hud, ""); setContext(scene, null); }
  function interact(scene) { if (scene.ahContext === "relay") activateRelay(scene); else if (scene.ahContext === "extract") completeRun(scene); else if (scene.ahContext === "archive") playStoryBeat(scene, "brokenLaneArchive"); else if (scene.ahContext === "cache") openCache(scene); }
  function updatePlayer(scene) { const run = scene.ahRun; if (!scene.ahPlayerControl || !(run?.status === "active" || run?.status === "cleared")) return scene.ahStop(); let x = scene.ahJoy.x, y = scene.ahJoy.y, k = scene.ahKeys, c = scene.ahCursors; if (k?.left?.isDown || c?.left?.isDown) x--; if (k?.right?.isDown || c?.right?.isDown) x++; if (k?.up?.isDown || c?.up?.isDown) y--; if (k?.down?.isDown || c?.down?.isDown) y++; const move = scene.ahMove.set(x, y); if (move.lengthSq() > 0) { move.normalize().scale(COMBAT.playerSpeed); scene.ahPlayer.setVelocity(move.x, move.y); setAnimation(scene.ahPlayer, "ah-relay-player", Math.abs(move.x) > Math.abs(move.y) ? (move.x < 0 ? "left" : "right") : (move.y < 0 ? "up" : "down"), true); } else scene.ahStop(); scene.ahPlayer.ahShadow?.setPosition(scene.ahPlayer.x, scene.ahPlayer.y + 36); }
  function showSlash(scene, enemy) { const p = scene.ahPlayer, angle = Math.atan2(enemy.y - p.y, enemy.x - p.x), x = p.x + Math.cos(angle) * 34, y = p.y + Math.sin(angle) * 34; if (scene.textures.exists(ASSETS.slash.key)) { const slash = scene.add.sprite(x, y, ASSETS.slash.key, 0).setScale(.32).setRotation(angle).setDepth(50); slash.play("ah-relay-slash"); slash.once("animationcomplete", () => slash.destroy()); return; } const slash = scene.add.arc(x, y, 24, 210, 510, false, 0x8cefff, 0).setStrokeStyle(4, 0xbaf6ff, .95).setDepth(50); scene.tweens.add({ targets: slash, alpha: 0, scaleX: 1.35, scaleY: 1.35, duration: 120, onComplete: () => slash.destroy() }); }
  function playerAttack(scene) { const run = scene.ahRun, time = scene.time.now; if (!isCombatEnabled(run) || !scene.ahPlayer?.active || time < scene.ahNextAttackAt) return; const target = nearestEnemy(scene); if (!target || target.distanceSq > COMBAT.playerAttackRange * COMBAT.playerAttackRange) return; if (target.enemy.ahCombat.type === "sentinel" && target.enemy.ahCombat.state !== "recovery") { showMessage(scene, "SENTINEL ARMOUR LOCKED"); return; } scene.ahNextAttackAt = time + COMBAT.playerAttackCooldownMs; showSlash(scene, target.enemy); damageEnemy(scene, target.enemy, run.combatSnapshot.meleeDamage); }
  function updateHowlControl(scene, time) { const remaining = global.AlphaSectorCombatConfig?.getHowlCooldownRemaining?.(scene, time) || 0, active = isCombatEnabled(scene.ahRun) && !!scene.ahPlayer?.active && !!scene.ahPlayerControl && scene.input?.enabled !== false, ready = active && remaining <= 0, maxCd = global.AlphaSectorCombatConfig?.HOWL_BURST?.cooldownMs || 6000, ratio = !active ? 0 : (ready ? 1 : 1 - remaining / maxCd); scene.ahHowlRing?.setAlpha(ready ? 1 : .38); scene.ahHowlIcon?.setAlpha(ready ? 1 : .4); scene.ahHowlGlow?.setAlpha(ready ? .85 : .15); scene.ahHowlLabel?.setAlpha(ready ? 1 : .52).setText(!active ? "HOWL" : ready ? "HOWL" : Math.ceil(remaining / 1000) + "s"); if (scene.ahHowlHit?.input) scene.ahHowlHit.input.enabled = ready; global.AlphaRf01ProductionHud?.updateHowl?.(S.hud, ratio, remaining, ready); }
  function tryHowlBurst(scene) { const run = scene.ahRun, time = scene.time.now; if (!isCombatEnabled(run) || !scene.ahPlayer?.active || !scene.ahPlayerControl) return false; const result = global.AlphaSectorCombatConfig?.triggerHowlBurst?.(scene, time, damageEnemy); if (!result?.activated) return false; run.howlUses += 1; showMessage(scene, result.affected ? "HOWL BURST - " + result.affected + " HOSTILE" + (result.affected === 1 ? " HIT" : "S HIT") : "HOWL BURST RELEASED"); updateHowlControl(scene, time); return true; }
  function damageEnemy(scene, enemy, damage) { if (!enemy?.active || enemy.ahCombat?.dead) return; enemy.ahCombat.hp = Math.max(0, enemy.ahCombat.hp - damage); enemy.setTint?.(0xffffff); const spark = scene.add.circle(enemy.x, enemy.y, 9, enemy.ahCombat.type === "shooter" ? 0xffca73 : 0xdffcff, .88).setDepth(48); scene.tweens.add({ targets: spark, scale: 2.3, alpha: 0, duration: 130, onComplete: () => spark.destroy() }); scene.time.delayedCall(70, () => enemy?.active && !enemy.ahCombat?.dead && enemy.clearTint?.()); updateEnemyUi(enemy); if (enemy.ahCombat.hp === 0) killEnemy(scene, enemy); }
  function killEnemy(scene, enemy) { if (!enemy?.active || enemy.ahCombat?.dead) return; const run = scene.ahRun, combat = enemy.ahCombat, state = run.encounters[combat.encounterId]; combat.dead = true; delete enemy.ahHowlKnockback; setEnemyVelocity(enemy, 0, 0); enemy.body.enable = false; enemy.ahTelegraph?.destroy?.(); enemy.ahTelegraph = null; enemy.ahRig?.forEach(item => item.destroy()); enemy.ahShadow?.destroy(); enemy.ahUi?.forEach(item => item.destroy()); scene.ahEnemyUi = scene.ahEnemyUi.filter(item => !enemy.ahUi?.includes(item)); if (combat.returnTrace) { enemy.ahVisual?.destroy?.(); resolveTraceReturnContact(scene); } else if (combat.type === "sentinel") { dropLoot(scene, enemy.x, enemy.y, 2); createPickup(scene, enemy.x + 18, enemy.y, "bones", 2); createPickup(scene, enemy.x - 18, enemy.y + 12, "scrap", 2); enemy.ahVisual?.setTexture?.(ASSETS.sentinelDefeated.key).setVisible(true); scene.time.delayedCall(700, () => enemy.ahVisual?.destroy?.()); } else { dropLoot(scene, enemy.x, enemy.y, combat.type === "shooter" ? 1 : 0); enemy.ahVisual?.destroy?.(); } if (!combat.returnTrace) { global.AlphaSectorCombatConfig.recordEnemyKill(run, combat.type, combat.required); state.remaining = Math.max(0, state.remaining - 1); if (state.remaining === 0) clearEncounter(scene, combat.encounterId); } scene.tweens.add({ targets: enemy, alpha: 0, scaleX: .32, scaleY: .32, duration: 170, onComplete: () => enemy.destroy() }); updateHud(scene); }
  function dropLoot(scene, x, y, bonus) { Object.entries(LOOT).forEach(([kind, config]) => { if (Math.random() > config.chance) return; createPickup(scene, x, y, kind, config.value + (kind === "exp" ? bonus * 4 : 0)); }); }
  function createPickup(scene, x, y, kind, value) { const config = LOOT[kind], pickup = scene.physics.add.sprite(x, y, ASSETS.loot.key, config.frame).setScale(.16).setDepth(24); pickup.body.setCircle(30, 98, 98); pickup.setVelocity((Math.random() - .5) * 110, -40 - Math.random() * 60).setDrag(260, 260).setMaxVelocity(100, 100); pickup.ahLoot = { kind, value, label: config.label, rarity: "common" }; if (scene.textures.exists(UI_TEX.beamC.key)) { pickup.ahBeam = scene.add.image(x, y + 10, UI_TEX.beamC.key).setDisplaySize(18, 36).setDepth(23).setAlpha(.82); } if (kind === "equipment" && scene.textures.exists(UI_TEX.label.key)) { pickup.ahLabel = scene.add.image(x, y - 22, UI_TEX.label.key).setDisplaySize(64, 18).setDepth(25).setAlpha(.9); pickup.ahTag = scene.add.text(x, y - 22, config.label, { fontFamily: "system-ui", fontSize: "8px", color: "#e8f7ff", fontStyle: "bold" }).setOrigin(.5).setDepth(26); } scene.ahLoot.add(pickup); pickup.once("destroy", () => destroyLootFx(pickup)); scene.tweens.add({ targets: pickup, angle: 360, duration: 950, repeat: -1 }); }
  function collectLoot(player, pickup) { if (!pickup?.active || !this.ahRun || (this.ahRun.status !== "active" && this.ahRun.status !== "cleared")) return; const reward = pickup.ahLoot, px = pickup.x, py = pickup.y; destroyLootFx(pickup); pickup.disableBody(true, true); pickup.destroy(); if (reward.kind === "exp") this.ahRun.expEarned += reward.value; if (reward.kind === "bones") this.ahRun.bonesEarned += reward.value; if (reward.kind === "scrap") this.ahRun.scrapEarned += reward.value; if (reward.kind === "equipment") this.ahRun.equipmentDrops += reward.value; if (this.textures.exists(UI_TEX.flash.key)) { const flash = this.add.image(px, py, UI_TEX.flash.key).setDisplaySize(34, 28).setDepth(49); this.tweens.add({ targets: flash, alpha: 0, scale: 1.5, duration: 170, onComplete: () => flash.destroy() }); } updateHud(this); showMessage(this, "+" + reward.value + " " + reward.label); }
  function clearEncounter(scene, id) { const run = scene.ahRun, encounter = ENCOUNTERS.find(e => e.id === id), state = run?.encounters?.[id]; if (!run || !encounter || !state || state.cleared) return; state.cleared = true; if (encounter.gate) openGate(scene, encounter.gate); if (id === "side_cache") { showMessage(scene, "SIDE CACHE UNSEALED"); scene.ahCacheGlow.setAlpha(.20); } else if (ENCOUNTERS.filter(e => e.required).every(e => run.encounters[e.id].cleared)) { run.status = "cleared"; setRelayState(scene, "available"); showMessage(scene, "RELAY READY"); playStoryBeat(scene, "relayReady"); } else showMessage(scene, encounter.name + " SECURED"); if (id === "signal_approach") storyTimer(scene, "signal_detected", 450, () => playStoryBeat(scene, "signalDetected")); updateHud(scene); updateContext(scene); }
  function openCache(scene) { const run = scene.ahRun; if (!run || run.cacheOpened || !run.encounters.side_cache.cleared) return; run.cacheOpened = true; scene.ahCache.setTint?.(0xbffaff).setAlpha?.(.58); scene.ahCacheGlow.setAlpha(.45); createPickup(scene, scene.ahCache.x - 25, scene.ahCache.y, "exp", 36); createPickup(scene, scene.ahCache.x + 10, scene.ahCache.y - 14, "bones", 3); createPickup(scene, scene.ahCache.x + 32, scene.ahCache.y + 10, "scrap", 3); if (Math.random() < .35) createPickup(scene, scene.ahCache.x, scene.ahCache.y + 26, "equipment", 1); showMessage(scene, "SIDE CACHE OPENED"); updateHud(scene); updateContext(scene); }
  function activateRelay(scene) { const run = scene.ahRun; if (!run || run.status !== "cleared" || scene.ahContext !== "relay" || run.relayActivated) return; run.relayActivated = true; setRelayState(scene, "activated"); playStoryBeat(scene, "relayActive"); updateHud(scene); updateContext(scene); }
  function buildCompletionPayload(run) { return global.AlphaSectorCombatConfig?.buildSectorCompletionPayload?.(run) || global.AlphaSectorCombatConfig?.completionPayload?.(run) || { runId: run?.runId, sectorId: run?.sectorId, runProfileVersion: run?.runProfileVersion, duration: Math.max(10, Number(run?.duration) || 10), relayActivated: run?.relayActivated === true, chasersKilled: run?.chasersKilled, shootersKilled: run?.shootersKilled, sentinelKilled: run?.sentinelKilled, mandatoryEnemiesKilled: run?.mandatoryEnemiesKilled, optionalEnemiesKilled: run?.optionalEnemiesKilled, damageTaken: run?.damageTaken, howlUses: run?.howlUses, cacheOpened: !!run?.cacheOpened, expEarned: run?.expEarned, bonesEarned: run?.bonesEarned, scrapEarned: run?.scrapEarned, equipmentDrops: run?.equipmentDrops }; }
  function normalizeCompletionResponse(result) { return global.AlphaSectorCombatConfig?.parseSectorCompletionResult?.(result) || null; }
  function logCompletionDiagnostic(stage, detail) { try { console.warn("[RF01_COMPLETE]", { build: RF01_BUILD, webapp: String(global.WEBAPP_VER || ""), stage, endpoint: "/webapp/world-exploration/complete-run", ...detail }); } catch (_) {} }
  function submitCompletionOnce(run) { if (!run || run.status !== "completed" || !run.relayActivated) return Promise.resolve(null); if (run.savePromise) return run.savePromise; S.completedRunIds.add(run.runId); run.saveState = "saving"; run.saveError = ""; showResultPanel(run, false); const post = global.AlphaRelayCompletionAdapter?.submit || (payload => S.apiPost ? S.apiPost("/webapp/world-exploration/complete-run", payload) : Promise.reject(new Error("completion_adapter_unavailable"))), payload = buildCompletionPayload(run); logCompletionDiagnostic("request", { sectorId: payload.sectorId, duration: payload.duration, mandatoryEnemiesKilled: payload.mandatoryEnemiesKilled, chasersKilled: payload.chasersKilled, shootersKilled: payload.shootersKilled, sentinelKilled: payload.sentinelKilled, expEarned: payload.expEarned, bonesEarned: payload.bonesEarned, scrapEarned: payload.scrapEarned, equipmentDrops: payload.equipmentDrops, relayActivated: payload.relayActivated, hasApiPost: typeof S.apiPost === "function" }); run.savePromise = Promise.resolve(post(payload)).then(result => { const body = normalizeCompletionResponse(result); if (!body) { run.saveError = global.AlphaSectorCombatConfig?.completionFailureCode?.(null, result) || "invalid_response"; throw new Error(run.saveError); } run.authoritativeRewards = body.rewards || {}; run.firstClear = body.firstClear === true; run.progressionEffects = body.progressionEffects || {}; run.saveState = "saved"; run.saveError = ""; logCompletionDiagnostic("saved", { firstClear: run.firstClear, rewards: run.authoritativeRewards }); if (S.scene?.ahRun?.runId === run.runId) { showResultPanel(run, false); void Promise.resolve(global.WorldExploration?.refreshState?.({ force: true })).finally(() => { if (S.scene?.ahRun?.runId === run.runId) showResultPanel(run, false); }); } return body; }).catch(error => { run.saveState = "failed"; run.saveError = global.AlphaSectorCombatConfig?.completionFailureCode?.(error, error?.data) || "reward_sync_failed"; run.savePromise = null; S.completedRunIds.delete(run.runId); logCompletionDiagnostic("failed", { code: run.saveError, status: error?.status || 0 }); if (S.scene?.ahRun?.runId === run.runId) showResultPanel(run, false); return null; }); return run.savePromise; }
  function completeRun(scene) { const run = scene.ahRun, hasReturnState = !!run && Object.prototype.hasOwnProperty.call(run, "returnChoice"); if (!run || run.status !== "cleared" || !run.relayActivated || (hasReturnState && (!run.returnChoice || !run.returnNarrativeResolved))) return; run.status = "completed"; run.duration = Math.max(10, Math.round((Date.now() - run.startedAt) / 1000)); run.saveState = run.saveState || "pending"; scene.ahPlayerControl = false; scene.ahSignalPing = null; global.AlphaSectorCombatConfig?.clearHowlBurstState?.(scene, false); enemyChildren(scene).forEach(enemy => setEnemyVelocity(enemy, 0, 0)); cleanupProjectiles(scene); setContext(scene, null); showResultPanel(run, false); submitCompletionOnce(run); }
  function damagePlayer(scene, damage) { const run = scene.ahRun; if (!isCombatEnabled(run)) return; const finalDamage = global.AlphaSectorCombatConfig?.incomingPlayerDamage?.(damage, run.combatSnapshot); if (!finalDamage) return; const activeRunId = run.runId; run.playerHP = Math.max(0, run.playerHP - finalDamage); run.damageTaken += finalDamage; scene.ahPlayer.setTint(0xc96b72); scene.time.delayedCall(90, () => scene.ahRun?.runId === activeRunId && scene.ahPlayer?.active && scene.ahPlayer.clearTint()); updateHud(scene); if (run.playerHP === 0) failRun(scene); }
  function updateChaser(scene, enemy, time) { const p = scene.ahPlayer, dx = p.x - enemy.x, dy = p.y - enemy.y, distanceSq = dx * dx + dy * dy; if (distanceSq <= COMBAT.enemyAttackRange * COMBAT.enemyAttackRange) { setEnemyVelocity(enemy, 0, 0); setAnimation(enemy, "ah-relay-chaser", enemy.ahDirection, false); if (time >= enemy.ahCombat.nextAttackAt) { enemy.ahCombat.nextAttackAt = time + enemy.ahCombat.cooldownMs; damagePlayer(scene, enemy.ahCombat.damage); } } else if (distanceSq <= COMBAT.enemyAggroRange * COMBAT.enemyAggroRange) { const d = Math.sqrt(distanceSq) || 1, vx = dx / d * COMBAT.enemySpeed, vy = dy / d * COMBAT.enemySpeed; setEnemyVelocity(enemy, vx, vy); setAnimation(enemy, "ah-relay-chaser", Math.abs(vx) > Math.abs(vy) ? (vx < 0 ? "left" : "right") : (vy < 0 ? "up" : "down"), true); } else { setEnemyVelocity(enemy, 0, 0); setAnimation(enemy, "ah-relay-chaser", enemy.ahDirection, false); } }
  function updateShooter(scene, enemy, time) { const p = scene.ahPlayer, dx = p.x - enemy.x, dy = p.y - enemy.y, d = Math.hypot(dx, dy) || 1; if (d > SHOOTER.aggroRange) { setEnemyVelocity(enemy, 0, 0); return; } let vx = 0, vy = 0; if (d > SHOOTER.preferredRange + 26) { vx = dx / d * SHOOTER.speed; vy = dy / d * SHOOTER.speed; } else if (d < SHOOTER.retreatRange) { vx = -dx / d * SHOOTER.speed; vy = -dy / d * SHOOTER.speed; } else if (time % 1700 < 90) { vx = -dy / d * SHOOTER.speed * .5; vy = dx / d * SHOOTER.speed * .5; } setEnemyVelocity(enemy, vx, vy); setAnimation(enemy, "ah-relay-chaser", Math.abs(dx) > Math.abs(dy) ? (dx < 0 ? "left" : "right") : (dy < 0 ? "up" : "down"), Math.abs(vx) + Math.abs(vy) > 1); if (d >= SHOOTER.retreatRange && d <= SHOOTER.aggroRange && time >= enemy.ahCombat.nextAttackAt) { enemy.ahCombat.nextAttackAt = time + enemy.ahCombat.cooldownMs; fireProjectile(scene, enemy, dx / d, dy / d); } }
  function fireProjectile(scene, enemy, nx, ny) { if (!enemy?.active || scene.ahRun?.status !== "active") return; const bolt = scene.add.circle(enemy.x + nx * 24, enemy.y + ny * 18, 8, 0xa94754, .88).setStrokeStyle(2, 0xd77a82, .78).setDepth(34); scene.physics.add.existing(bolt); bolt.body.setCircle(8); bolt.body.setVelocity(nx * SHOOTER.projectileSpeed, ny * SHOOTER.projectileSpeed); bolt.body.setAllowGravity(false); bolt.ahDamage = enemy.ahCombat?.damage || SHOOTER.damage; bolt.ahExpiresAt = scene.time.now + 3600; scene.ahProjectiles.add(bolt); scene.tweens.add({ targets: bolt, alpha: .52, scale: 1.18, duration: 150, yoyo: true, repeat: -1 }); }
  function projectileHitSolid(projectile) { destroyProjectile(projectile); }
  function hitProjectile(player, projectile) { if (!projectile?.active || projectile.ahHit) return; projectile.ahHit = true; const damage = projectile.ahDamage || SHOOTER.damage; destroyProjectile(projectile); damagePlayer(this, damage); }
  function destroyProjectile(projectile) { if (!projectile?.active) return; projectile.body?.stop?.(); projectile.destroy(); }
  function cleanupProjectiles(scene) { scene.ahProjectiles?.clear(true, true); }
  function updateSentinel(scene, enemy, time) { const p = scene.ahPlayer, c = enemy.ahCombat; if (c.state === "idle") { const dx = p.x - enemy.x, dy = p.y - enemy.y, d = Math.hypot(dx, dy) || 1; if (d > 70) setEnemyVelocity(enemy, dx / d * SENTINEL.chaseSpeed, dy / d * SENTINEL.chaseSpeed); else setEnemyVelocity(enemy, 0, 0); if (time >= c.stateUntil) { c.state = "telegraph"; c.stateUntil = time + SENTINEL.telegraphMs; c.vector = { x: dx / d || 1, y: dy / d || 0 }; enemy.ahTelegraph = scene.add.circle(enemy.x, enemy.y, 48, 0xa83f4a, .08).setStrokeStyle(3, 0xc35a63, .64).setDepth(24); } } else if (c.state === "telegraph" && time >= c.stateUntil) { enemy.ahTelegraph?.destroy(); enemy.ahTelegraph = null; c.state = "lunge"; c.stateUntil = time + SENTINEL.lungeMs; c.hitThisLunge = false; setEnemyVelocity(enemy, c.vector.x * SENTINEL.lungeSpeed, c.vector.y * SENTINEL.lungeSpeed); enemy.ahVisual?.setTexture?.(ASSETS.sentinelAttack.key); } else if (c.state === "lunge") { if (!c.hitThisLunge && inRange(enemy, p, 76)) { c.hitThisLunge = true; damagePlayer(scene, c.damage); } if (time >= c.stateUntil) { setEnemyVelocity(enemy, 0, 0); c.state = "recovery"; c.stateUntil = time + c.recoveryMs; enemy.ahVisual?.setTexture?.(ASSETS.sentinelIdle.key); } } else if (c.state === "recovery" && time >= c.stateUntil) { c.state = "idle"; c.stateUntil = time + 350; } }
  function updateEnemy(scene, enemy, time) { const run = scene.ahRun; if (!isCombatEnabled(run) || !scene.ahPlayer?.active || !enemy?.active || enemy.ahCombat?.dead) return; if (global.AlphaSectorCombatConfig?.updateHowlKnockback?.(enemy, time)) { updateEnemyUi(enemy); return; } if (enemy.ahCombat.type === "sentinel") updateSentinel(scene, enemy, time); else if (enemy.ahCombat.type === "shooter") updateShooter(scene, enemy, time); else updateChaser(scene, enemy, time); updateEnemyUi(enemy); }
  function updateCombat(scene, time) { const target = nearestEnemy(scene); if (target) scene.ahTargetRing.setPosition(target.enemy.x, target.enemy.y + 18).setVisible(true); else scene.ahTargetRing.setVisible(false); for (const enemy of enemyChildren(scene)) { updateEnemy(scene, enemy, time); setEnemyUiFocus(enemy, target?.enemy === enemy); } for (const projectile of scene.ahProjectiles?.getChildren?.() || []) if (projectile?.active && (time >= projectile.ahExpiresAt || projectile.x < 0 || projectile.x > WORLD.width || projectile.y < 0 || projectile.y > WORLD.height)) destroyProjectile(projectile); }
  function failRun(scene) { const run = scene.ahRun; if (!isCombatEnabled(run)) return; run.status = "failed"; run.relayActivated = false; scene.ahSignalPing = null; scene.ahPlayerControl = false; global.AlphaSectorCombatConfig?.clearHowlBurstState?.(scene); enemyChildren(scene).forEach(enemy => setEnemyVelocity(enemy, 0, 0)); cleanupProjectiles(scene); clearStoryUi(scene); scene.ahPlayer.ahShadow?.setVisible(false); scene.ahPlayer.disableBody(true, true); scene.input.enabled = false; setContext(scene, null); showResultPanel(run, true); }
  function removeResultPanel() { setResultChrome(false); global.AlphaRf01ProductionHud?.removeResult?.(S.root); S.root?.querySelector(".ah-sector-result")?.remove(); }
  function returnResult(run) { return run?.returnChoice === "trace" ? { title: "SIGNAL TRACE", summary: "PARTIAL · DEEPER FRINGE · RF02", lines: ["It's deeper."] } : { title: "LINK SEVERED", summary: "SIGNAL STATUS: UNRESOLVED · RF02", lines: ["We cut the link.", "It's still there."] }; }
  function showResultPanel(run, failed) { setResultChrome(true); const branchStoryResult = failed ? null : returnResult(run); if (global.AlphaRf01ProductionHud?.renderResult) { global.AlphaRf01ProductionHud.renderResult(S.root, { failed: failed === true, saveState: run.saveState || (failed ? "" : "pending"), saveError: run.saveError || "", duration: run.duration || 0, kills: run.mandatoryEnemiesKilled || 0, exp: run.expEarned || 0, bones: run.bonesEarned || 0, scrap: run.scrapEarned || 0, gear: run.equipmentDrops || 0, rewards: run.authoritativeRewards || null, firstClear: !!(run.firstClear && run.progressionEffects?.relay7Activated), allowRf02: canOfferRf02(), branchStoryResult }); return; } removeResultPanel(); setResultChrome(true); const panel = document.createElement("section"); panel.className = "ah-sector-result"; const reward = run.authoritativeRewards || run, state = run.saveState === "saved" ? "SAVED" : run.saveState === "failed" ? ("SAVE FAILED" + (run.saveError ? " [" + run.saveError + "]" : "")) : "SECURING SIGNAL...", firstClear = run.firstClear && run.progressionEffects?.relay7Activated ? "<p><strong>RELAY STABILIZED</strong><br>RELAY-7 SIGNAL ONLINE</p>" : "", storyHook = failed ? "" : `<p><strong>${branchStoryResult.title}</strong><br>${branchStoryResult.summary}</p><p>${branchStoryResult.lines.join("<br>")}</p>`, continueBtn = !failed && run.saveState === "saved" && canOfferRf02() ? `<button type="button" data-sector-run-action="continue">CONTINUE TO RF02</button>` : ""; panel.innerHTML = failed ? `<div class="ah-sector-result__card ah-sector-result__card--failed"><div class="ah-sector-result__eyebrow">RELAY FRINGE 01</div><h2>RUN FAILED</h2><p>The signal run collapsed. No local rewards were claimed.</p><div class="ah-sector-result__actions"><button type="button" data-sector-run-action="restart">RESTART SECTOR</button><button type="button" data-sector-run-action="map">RETURN TO MAP</button></div></div>` : `<div class="ah-sector-result__card"><div class="ah-sector-result__eyebrow">RUN COMPLETE</div><h2>${SECTOR.displayName}</h2>${firstClear}<p>${state}</p>${storyHook}<dl><dt>Time</dt><dd>${run.duration}s</dd><dt>Mandatory hostiles</dt><dd>${run.mandatoryEnemiesKilled} / 16</dd><dt>EXP</dt><dd>+${reward.exp ?? reward.expEarned ?? 0}</dd><dt>Bones</dt><dd>+${reward.bones ?? reward.bonesEarned ?? 0}</dd><dt>Scrap</dt><dd>+${reward.scrap ?? reward.scrapEarned ?? 0}</dd><dt>Equipment</dt><dd>${run.equipmentDrops}</dd></dl><div class="ah-sector-result__actions">${run.saveState === "failed" ? `<button type="button" data-sector-run-action="retry">RETRY SAVE</button>` : ""}${continueBtn}<button type="button" data-sector-run-action="again">RUN AGAIN</button><button type="button" data-sector-run-action="map">RETURN TO MAP</button></div><p style="margin:10px 0 0;color:#6f8492;font-size:9px;letter-spacing:.08em">${RF01_BUILD}</p></div>`; S.root?.querySelector(".ah-exploration-room__stage")?.appendChild(panel); }
  function shutdown() { const scene = this; scene.ahResetInput?.(); scene.ahStop?.(); scene.ahJoyHit?.off("pointerdown", scene.ahOnJoyDown); scene.ahAttackHit?.off("pointerdown", scene.ahOnAttackDown); scene.ahHowlHit?.off("pointerdown", scene.ahOnHowlDown); scene.ahInteractHit?.off("pointerdown", scene.ahOnInteractDown); scene.input?.off("pointermove", scene.ahOnMove); scene.input?.off("pointerup", scene.ahOnUp); scene.input?.off("gameout", scene.ahOnGameOut); scene.ahMessageTimer?.remove?.(false); clearStoryUi(scene); global.AlphaSectorCombatConfig?.clearHowlBurstState?.(scene); cleanupProjectiles(scene); scene.ahTelegraph?.destroy?.(); removeResultPanel(); }
  function preload() { trace("SCENE_PRELOAD"); this.load.on("loaderror", file => { try { console.warn("[RF01] optional asset failed", file?.key || file); } catch (_) {} }); Object.values(ASSETS).filter(asset => ![ASSETS.player, ASSETS.chaser, ASSETS.loot, ASSETS.slash].includes(asset)).forEach(asset => this.load.image(asset.key, asset.url)); Object.values(UI_TEX).forEach(asset => this.load.image(asset.key, UI_BASE + asset.file)); Object.values(STORY_ASSETS).forEach(asset => this.load.image(asset.key, asset.url)); this.load.spritesheet(ASSETS.player.key, ASSETS.player.url, { frameWidth: 256, frameHeight: 256 }); this.load.spritesheet(ASSETS.chaser.key, ASSETS.chaser.url, { frameWidth: 256, frameHeight: 256 }); this.load.spritesheet(ASSETS.loot.key, ASSETS.loot.url, { frameWidth: 256, frameHeight: 256 }); this.load.spritesheet(ASSETS.slash.key, ASSETS.slash.url, { frameWidth: 256, frameHeight: 256 }); }
  function create() { try { trace("SCENE_CREATE_START"); this.physics.world.setBounds(0, 0, WORLD.width, WORLD.height); makeAnimations(this); buildWorld(this); createControls(this); createKeyboard(this); this.cameras.main.setBounds(0, 0, WORLD.width, WORLD.height); this.cameras.main.startFollow(this.ahPlayer, true, .13, .13); this.cameras.main.setRoundPixels(true); this.sys.events.once("shutdown", shutdown, this); S.scene = this; this.ahLayout?.(S.canvasParent?.clientWidth || this.scale.width, S.canvasParent?.clientHeight || this.scale.height, S.safeInsets); startRun(this); const canvas = S.canvasParent?.querySelector("canvas") || this.game.canvas; if (!canvas) throw new Error("Relay canvas was not created"); canvas.setAttribute("aria-label", "Relay Fringe 01 playable sector run"); trace("ROOM_READY"); settle(true); } catch (error) { settle(false, error); } }
  function updateLootFx(scene) { const kids = scene.ahLoot?.getChildren?.(); if (!kids || !kids.length) return; for (const pickup of kids) { if (!pickup?.active) continue; pickup.ahBeam?.setPosition(pickup.x, pickup.y + 10); pickup.ahLabel?.setPosition(pickup.x, pickup.y - 22); pickup.ahTag?.setPosition(pickup.x, pickup.y - 22); } }
  function update(time) { if (S.closing || this.ahPaused || !this.ahPlayer) return; updatePlayer(this); activateEncounters(this); updateReturnPhase(this); updateCombat(this, time); updateHowlControl(this, time); updateContext(this); updateLootFx(this); updateRadar(this); const k = this.ahKeys; if (global.Phaser.Input.Keyboard.JustDown(k?.attack) || global.Phaser.Input.Keyboard.JustDown(k?.space)) playerAttack(this); if (global.Phaser.Input.Keyboard.JustDown(k?.howl)) tryHowlBurst(this); if (global.Phaser.Input.Keyboard.JustDown(k?.interact)) interact(this); if (global.Phaser.Input.Keyboard.JustDown(k?.escape)) void close(); }
  function settle(ok, error) { const resolve = S.resolveReady, reject = S.rejectReady; S.resolveReady = S.rejectReady = null; if (S.readyTimer) { clearTimeout(S.readyTimer); S.readyTimer = null; } if (ok) resolve?.(true); else reject?.(error || new Error("Relay room unavailable")); }
  function createGame() { return new Promise((resolve, reject) => { S.resolveReady = resolve; S.rejectReady = reject; S.readyTimer = setTimeout(() => settle(false, new Error("Relay room timed out")), 9000); try { S.game = new global.Phaser.Game({ type: global.Phaser.AUTO, parent: CANVAS_ID, width: S.canvasParent.clientWidth, height: S.canvasParent.clientHeight, backgroundColor: "#071019", audio: { noAudio: true }, input: { activePointers: 4, touch: { capture: true } }, scale: { mode: global.Phaser.Scale.RESIZE, width: S.canvasParent.clientWidth, height: S.canvasParent.clientHeight }, physics: { default: "arcade", arcade: { gravity: { x: 0, y: 0 }, debug: !!global.DBG } }, render: { roundPixels: true, powerPreference: "low-power" }, scene: { preload, create, update } }); } catch (error) { settle(false, error); } }); }
  async function cleanup(options) { settle(false); unbindEvents(); const scene = S.scene; scene?.ahResetInput?.(); scene?.ahStop?.(); cleanupProjectiles(scene); if (scene?.input) scene.input.enabled = false; const game = S.game; S.game = null; S.scene = null; if (game) try { game.destroy(true); } catch (error) { log("destroy", error); } S.canvasParent?.replaceChildren(); unregisterNavigation(!!options?.fromNavigation); S.root?.remove(); S.root = S.canvasParent = S.loading = S.hud = null; S.hudFingerprint = ""; S.radarFingerprint = ""; S.radarAt = 0; S.ready = false; restorePage(); }
  async function runOpen(generation) { try { injectCSS(); buildShell(); lockPage(); readInsets(); bindEvents(); registerNavigation(); if (!global.Phaser?.Game) throw new Error("Phaser unavailable"); const ready = await createGame(); if (!ready || generation !== S.generation || S.closing) return false; S.loading.hidden = true; S.ready = true; return true; } catch (error) { await cleanup(); throw error; } }
  function init(deps = {}) { S.tg = deps.tg || S.tg || global.Telegram?.WebApp || null; S.apiPost = deps.apiPost || S.apiPost; if (typeof deps.dbg === "boolean" || typeof deps.dbg === "function") S.dbg = deps.dbg; if (typeof deps.trace === "function") S.trace = deps.trace; try { console.info("[RF01]", RF01_BUILD, { webapp: String(global.WEBAPP_VER || ""), hasApiPost: typeof S.apiPost === "function" }); } catch (_) {} return API; }
  function open(options = {}) { if (String(options.sectorId || "") !== SECTOR.sectorId) return Promise.resolve(false); const combatProfile = global.AlphaSectorCombatConfig?.normalizeCombatProfile?.(options.combatProfile), threat = global.AlphaSectorCombatConfig?.normalizeThreatTier?.(options.threatTier); if (!combatProfile || !threat) return Promise.reject(new Error("Unable to load combat profile.")); if (S.opening) return S.opening; if (isOpen() && S.ready) return Promise.resolve(true); S.combatProfile = combatProfile; S.threatTier = threat.id; S.resolveCombatProfile = typeof options.resolveCombatProfile === "function" ? options.resolveCombatProfile : null; S.onClose = typeof options.onClose === "function" ? options.onClose : null; S.closing = false; const generation = ++S.generation, task = runOpen(generation), wrapped = task.finally(() => { if (S.opening === wrapped) S.opening = null; }); S.opening = wrapped; return wrapped; }
  async function close(options) { if (S.closing) return false; if (!S.root && !S.game && !S.opening) return false; S.closing = true; ++S.generation; const after = S.onClose; S.onClose = null; try { await cleanup(options); await Promise.resolve(after?.()); return true; } finally { S.closing = false; } }
  function isOpen() { return !!(S.root && document.documentElement.contains(S.root) && !S.closing); }
  const LegacyAPI = { init, open, close, isOpen, sectorConfig: SECTOR, encounterConfig: ENCOUNTERS, shooterConfig: SHOOTER };
  LegacyAPI.worldConfig = WORLD;
  const API = global.AlphaActionSectorRuntime?.create?.({ legacyApi: LegacyAPI }) || LegacyAPI;
  global.AlphaExplorationRoom = API;
})(window);
